建立一份“KRAS G12C 抑制剂的可复查研发情报”小型 dossier。只使用 PureScience 的公开科学连接器和本地 Python，不要凭模型记忆补全数据。

1. 用 ChEMBL 查找已报道、靶向 KRAS G12C 的小分子抑制剂；只保留明确的 IC50 记录，按数值升序取最多 5 个候选。每条保留化合物名、ChEMBL ID、assay/target 描述、IC50、单位、文献或 assay 标识。若目标或突变注释无法精确筛出，要说明实际筛选逻辑与限制。
2. 对每个候选用 ClinicalTrials.gov 查询 KRAS G12C 或相应药物的肿瘤临床试验，记录最多一个可定位的试验：NCT 编号、阶段、总体状态、申办方。没有可靠匹配时写“未找到”，不要推断。
3. 用 PubMed 为每个最终候选找最多 2 篇论文，至少包含 PMID、标题、期刊、年份；优先机制或临床证据。
4. 用 Python 将结果写成 `kras_g12c_inhibitors.csv`，生成 `kras_g12c_ic50.png`（清晰标注单位及对数坐标是否使用），并写 `kras_g12c_dossier.md`。报告必须单列“已由连接器核实”和“未找到/不能核实”的内容。
5. 同时交付生成 CSV 和图表的 Python 脚本。不要给出治疗建议或患者层面的结论。

完成前自查：所有交付文件可在项目文件库中打开；每个关键记录均有可追溯 ID；不要把缺失数据写成空泛结论。
