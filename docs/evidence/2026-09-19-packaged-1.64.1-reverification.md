# 打包版复测：v1.64.1 的首屏扇出、产物探测与存储首读（生产构建）

- 日期：2026-09-19
- 构建：`dist/mac-arm64/PureScience.app`，本地 `npm run build:unpack` 产出，`CFBundleShortVersionString = **1.64.1**`，对应源码 **`433a015`（发版提交）**
  - ⚠️ 该产物**不含** `5e6afec`（发版后那笔"迁移句也显示测量中"的 UI 文案修复）—— 它晚于本次构建。
- 语料：**真实数据根的只读副本**，且按**打包布局**构造（`<root>/PureScience/{artifacts,notebooks,workspaces,inputs,uploads}` + `<root>/{purescience.db,sessions,settings.json,web-token,skills,claude,runtime,…}`）—— 这是上一次打包复测读数为 0 的原因（dev 布局是 `PureScience-DEV/`）。
- 隔离：独立端口 **44106** + 独立 `--user-data-dir` + `PURESCIENCE_E2E_STORAGE_ROOT=<沙箱>`；**运行前与运行中 44100 均无监听**（本轮 KeepAlive 没有来抢）。
- 驱动：CDP 直调 `storage.getInfo()`（三次）+ 首页 chip 渲染 + 产物最密集项目（31 产物）与文件面板驱动。

## 一、结果（判据逐项）

| 判据 | 打包版实测 | 判定 |
|---|---|---|
| 存储首读不再阻塞 | 第 1 次 **1 ms / `pending: true`**；5 s 后仍 pending；**20 s 后 `pending: false` / 7,363,622,681 B（7.36 GB）/ 5 类** | ✅ 契约在生产构建上成立 |
| 真机 chip 一致 | **177 元素 / 16 种**（`CSV/GZ/HTML/IPYNB/JSON/LOG/MD/NPZ/PDB/PDF/PNG/PY/SDF/SVG/TXT/XLSX`），与 dev 逐项一致 | ✅ |
| chip 相关慢读归零 | `listFiles segments were slow` **0**、`project file kinds read was slow` **0** | ✅ |
| 产物读不慢 | `project files read was slow` **0**、`artifact version resolve was slow` **0** | ✅ |
| 其它慢通道 | `ipc handler was slow` 14 条（未指名通道；与 dev 构建同量级） | 记录 |

## 二、隔离（按上一轮的教训做的）

1. **真实根写入扫描**：本轮窗口（约 19:52–20:00）内 `~/.purescience-project` **无任何文件被写**；此前 19:48:37/38 的写入是**上一次收尾 `launchctl load` 后常驻实例自身的启动写**（`web-service.json` 登记 port 44100 / pid 22970 = 当前常驻进程，与本次被测实例的 44106 不同）。
2. **日志路径法**：打包版日志里真实配置根 `/Users/totota/.purescience-project` 出现 **0** 次、真实数据根 `/Users/totota/PureScience-DEV` 出现 **0** 次。
   - 诚实说明：本轮该判据**偏弱** —— 沙箱根路径在日志里也是 0 次（这次没有打印带路径的行），所以"路径法"这次没有给出正证据；真正的证据是第 1 条的写入扫描为空 + 存储读数 **7.36 GB** 与沙箱语料一致（真实根若被读到不会是这个数）。

## 三、诚实边界

1. **IPC 调用计数无法在打包版取得**：`52 → 1`（Home 扇出）与 `40 → 0/2 → 1`（产物探测）依赖临时追踪仪（`PS_IPC_TRACE`，**不随发行版发出**）。打包版只能验证**用户可见结果**（chip 数、慢读报告、存储读数）—— 这一分层已写在交接档。
2. 本档只覆盖 **macOS arm64**。
3. 产物对应 `433a015`，不含 `5e6afec` 的 UI 文案修复（无功能差异）。
