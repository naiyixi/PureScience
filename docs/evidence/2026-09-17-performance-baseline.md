# 流畅度实测基线（2026-09-17，真实实例 / 真实数据）

用户反馈"卡"。按"先量数字再谈优化"做基线，**全部数字来自运行中的真实实例与真实数据根**（`~/.purescience-project`，59 个会话 / 51 个项目目录 / `sessions` 共 **60 MB**）。

## 一、基线数字（同一进程、连续多次调用）

| 动作 | 实测 | 说明 |
|---|---|---|
| **`sessions:load-all`（会话目录整体加载）** | **1671 / 1302 / 1360 / 1366 / 1492 ms**（中位 ~1366） | 每次调用都这个量级，**看不到缓存** |
| ↳ 该通道**返回体量** | **55,371,532 B ≈ 55 MB** | 一次"列表"调用返回 55 MB |
| ↳ 磁盘上对应 JSON | 62,919,464 B ≈ 63 MB | 源数据规模 |
| ↳ **同样这些字节从磁盘读**（page cache） | **10 ms**（59 文件 / 62.8 MB） | ⇒ 瓶颈**不在 I/O** |
| `search:query`（中文"稳定性"） | **1169 ms 首次 / 62 / 69 ms 之后** | 索引预热后 ~65 ms，可接受 |
| `artifacts:list-project-files`（产物+未发布条目） | 72 / 39 / 37 / 46 / 56 ms | 可接受 |
| 启动协调 `session-hydration`（日志实测） | **1270 / 1359 / 1604 ms**，启动那趟曾见 **6754 ms** | 每趟都遍历 59 个会话 |

## 二、烟枪：一次"列表"调用搬 55 MB

- `load-all` 的中位 **1.37 s**，而**同样的 62.8 MB 从磁盘读只要 10 ms** ⇒ 时间花在**解析/归一化/序列化**这 60 MB 上，而不是读盘；
- 返回值 **55 MB** —— 一个"会话目录"接口把**全部消息体**一起吐回来。这就是"卡"的最大嫌疑：任何需要会话列表的路径（启动、切换、刷新）都要付这笔钱。

## 三、我没能量到的（如实记，不粉饰）

| 想量的 | 情况 |
|---|---|
| `project-files:list-files`（文件面板） | 我给的参数形状不对 ⇒ `handler_error: Cannot read properties of undefined (reading 'trim')`；**未取到数字** |
| `artifacts:list`（会话级产物） | web RPC 上 `method_not_found`（Electron 专有面）⇒ **未取到数字** |
| **渲染层流畅度**（切页、滚动、CSS 动画帧耗时） | 当前 dev 实例是 **headless**（无窗口），**渲染层指标无法从这里量** ⇒ 需要带窗口的实例 + CDP 采样（属独立量测任务） |
| 冷启动到可交互 | 本会话观察到 **10 / 20 / 20 / 30 s**（一次 60 s），但**这是 dev 构建**（含 electron-vite 重建），**不能当作打包版启动时间**，故不列为基线数字 |

## 五、55 MB 的构成（量出来的，和直觉相反）

对**最大的 12 个会话文件**（合计 25.6 MB / 634 条消息）逐字段拆字节：

| 字段 | 体积 | 占比 |
|---|---|---|
| `session.conversationGraph` | 11.35 MB | **44.3%** |
| `session.activities` | 6.82 MB | **26.6%** |
| `session.messages` | 3.75 MB | 14.6% |
| ↳ 其中 `eventIds` | 3.39 MB | **13.2%** |
| ↳ 其中 `content`（**正文**） | **0.14 MB** | **0.5%** |
| `session.artifacts` | 0.09 MB | 0.3% |

单条消息平均 5.8 KB，但**正文只占其中极小一块**：真正的体积在**会话图**、**活动流**和**消息上挂的 event id 簿记**。⇒ "列表接口把消息体一起吐回来"这个说法只对了一小半；**要瘦的是图与活动流**。

## 六、优化设计（下一单元，按此实施）

**两级目录**（不是把消息砍掉那么简单，且必须防止存盘截断）：

1. **列表层**：`load-all` 只回列表所需（id/标题/项目/时间/**计数**/最后活动摘要），**不带** `conversationGraph`、`activities`、每消息 `eventIds`。目标：**< 2 MB、p95 < 150 ms**。
2. **按需层**：选中会话时拉完整文档（新增读取通道），并入现有 store。
3. **安全护栏（关键）**：store 里必须区分"完整加载"与"仅摘要"的会话，且 **saver 绝不允许把仅摘要的会话写回磁盘**（现在的 saver 是"引用变了就落盘"，部分会话在内存里会**静默截断**用户的会话文件）。护栏要有用例：仅摘要会话 + 一次编辑 ⇒ 要么先补齐再保存，要么**具名拒绝**，绝不静默覆盖。
4. **主进程目录缓存 + 变更事件失效**（`session:created/updated/deleted` 事件面已有）：连续两次调用第二次 **< 20 ms**，改动后立刻反映。
5. **启动协调增量**：只处理水位之后变化的会话，启动那趟 **< 1 s**。

**验收口径（同一数据根、同一量法复测）**：`load-all` 响应 < 2 MB 且 p95 < 150 ms；第二次调用 < 20 ms；启动协调 < 1 s；**且**"仅摘要会话永不落盘"有真机证据。

## 七、结论与下一步（优化是独立单元，先立案）

**结论**：`卡` 的第一嫌疑是**会话目录一次性搬运 55 MB / 1.3 s**，且启动那趟还要叠加协调（1.3–6.7 s）。I/O 已被排除（10 ms）。

**下一步（v1.64，带验收口径）**：
1. **列表不再返回全部消息体**：`load-all` 只回摘要（标题/时间/计数/最后一条片段），消息按需拉取 —— 验收：该通道响应 **< 2 MB**、耗时 **p95 < 150 ms**（同一数据根、同一量法复测）；
2. **目录级缓存 + 变更事件失效**（`session:created/updated/deleted` 已有事件面）—— 验收：连续两次调用第二次 **< 20 ms**，且改动后能立刻反映（用例 + 真机各一次）；
3. **启动协调增量**：只处理自上次水位以来变化的会话 —— 验收：启动那趟 **< 1 s**（59 会话数据根）。
4. **渲染层**：另起一次带窗口实例的 CDP 采样（切页 / 滚动 / 大列表），把"用户感受到的卡"也变成数字。
