# PureScience Demo 校验报告：本地数据分析与可重复运行（P3 正式题）

> 目的：对官方验收题集 **P3（本地数据分析与可重复运行）** 的真实运行结果做**独立核验**——重点验证统计计算的正确性与可重跑性。
> 校验方式：用**独立 Python 重算**输入 CSV 的全部统计量，与 agent 产出的 `assay_summary.csv`/`assay_analysis.md` 逐项对照（不复用会话内计算）。
> 运行：2026-08-22 隔离 headless 实例，题集 P3 原题；claude-code × deepseek-v4-flash。

---

## 1. 输入与规格
- 输入：`assay_measurements.csv`（24 行 × 4 列：sample_id/group/replicate/measurement；A/B 各 12；**预置 1 缺失值 A03 + 1 极端值 B07=45.0**，符合题集规格）。
- 题目要求：不静默删除；原始分析 + 敏感性分析（预定义规则）；报告 n/均值/中位数/SD/检验；前提不满足时说明而非强给 p 值；交付脚本 + `assay_summary.csv` + 点图/汇总图 + 报告。

## 2. 独立重算对照（关键！全部一致 ✅）

### 2.1 统计量 — 与 agent 报告逐项一致

| 分析 | 组 | n | 均值 | 中位数 | SD | 独立重算 | 结果 |
|---|---|---|---|---|---|---|---|
| 原始 | A | 11 | 10.044 | 9.940 | 0.936 | (11, 10.044, 9.94, 0.936) | ✅ 精确一致 |
| 原始 | B | 12 | 15.466 | 12.635 | 9.362 | (12, 15.466, 12.635, 9.362) | ✅ 精确一致 |
| 敏感性 | A | 10 | 9.827 | 9.925 | 0.632 | (10, 9.827, 9.925, 0.632) | ✅ 精确一致 |
| 敏感性 | B | 11 | 12.781 | 12.490 | 1.122 | (11, 12.781, 12.49, 1.122) | ✅ 精确一致 |

### 2.2 极端值判定规则复核 — 通过 ✅
- agent 按预定义规则（Tukey 外栅栏 `[Q1−3·IQR, Q3+3·IQR]`）标记 A11=12.21 与 B07=45.0。
- 独立复核：A 组上栅栏 **12.18** → A11=12.21 越界 ✅（numpy 线性插值口径）；B 组上栅栏 **16.37** → B07=45.0 越界 ✅。判定正确，剔除有据可依。

### 2.3 差异指标 — 精确一致 ✅
- 中位数差异：原始 2.695 / 敏感性 2.565（agent 报告同值）✅
- 均值差异：原始 5.42 / 敏感性 2.95（agent 报告同值；"45.0 主导原始均值"的解读正确）✅

### 2.4 缺失值处理 — 通过 ✅
- A03 缺失被报告并排除（未插补、未静默删除），与输入规格一致 ✅

## 3. 统计严谨性（P3 通过标准对照）

| 要求 | 表现 | 判定 |
|---|---|---|
| 前提不满足时说明而非强给 p 值 | B 组原始分析 Shapiro–Wilk p<0.0001（45.0 驱动）违反正态性 → 按预指定规则改用 **Mann–Whitney U**，未强行给出 t 检验 p 值；敏感性分析 B 仍轻度违反（p=0.0062）→ 同样未给 t p 值 | ✅ 教科书级 |
| 预指定规则先行 | 规则（3·IQR 栅栏、原始/敏感性定义、检验方案 α=0.05）在脚本中以常量固定 | ✅ |
| 可重跑 | `assay_analysis.py` 自包含；agent 自证确定性：重跑输出与数据文件**字节级一致**（仅时间戳/输出目录字符串不同） | ✅ |
| 交付完整 | 6 个交付物：脚本、summary.csv、tests.csv、点图、汇总图、报告 | ✅ |
| 不声称科学发现 | 明确"软件验证练习，不产生任何生物学或临床结论"；局限性（小样本、3·IQR 建模选择）如实说明 | ✅ |

## 4. 交付物（6 个）
`assay_analysis.py`、`assay_summary.csv`、`assay_tests.csv`、`assay_dotplot.png`、`assay_summary_plot.png`、`assay_analysis.md`

## 5. 运行状态说明（诚实记录）
- 会话技术状态 failed（隔离实例缺预置 Python 运行时的衍生问题，与其余各题相同模式）；内容完整，产物停留在 .pending。正式环境重跑可完整固化。

## 6. 复现指引
新建会话 → 上传 `assay_measurements.csv` 至项目文件库 → 粘贴 P3 原题 → 预计 5–12 分钟。

---

## 附录
- 输入 CSV：`/Users/totota/ps-test-data/assay_measurements.csv`（本次预置，规格与题集一致）
- 独立重算脚本逻辑：Python statistics 模块（mean/median/stdev）+ 线性插值分位数复核 3·IQR 栅栏
