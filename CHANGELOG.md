# PureScience 更新日记

PureScience 是一款面向科学研究的开源 AI 工作台：多智能体协同、科学计算环境、
专才市场与可审计产物，服务于分子生物学、基因组学、药物研发等科研场景。
我们坚持独立原创、开放共享——愿以开源之力，造福全人类。

## 版本代号

每一版都有一个取自中华科学文明的中文代号：

| 代号 | 含义 | 版本 |
| --- | --- | --- |
| 浑仪 | 张衡浑天仪，窥天测地、度量万象——正式版里程碑 | v1.0.0 |
| 启明 | 启明星，长夜之后的第一缕光——品牌独立的起点 | v0.13.0 |
| 星火 | 星星之火，可以燎原——功能与生态的播种期 | v0.12.x |
| 神农 | 尝百草，辨药性——生物医药方向的未来篇章 | （规划） |
| 天工 | 天工开物——工具与工程体系的未来篇章 | （规划） |
| 北斗 | 指路星辰——导航与检索体系的未来篇章 | （规划） |

## Unreleased — 2026-09-02（体验与本地化批次）

- **设置导航一屏可扫**：左轨行高 32→28px、分组标题间距收紧、左轨独立滚动不裁切，面板高度提升——900px 窗口下 19 个设置项不再"滑几屏才找得到/只能靠搜索"（搜索保留为增强而非唯一路径）
- **标签可独立创建**：标签页"新建标签"不再静默 no-op——零资源标签真正持久化并自动选中，右侧显示"暂无资源携带此标签"空态（随后可在技能/连接器/专才详情页打上资源）；同步修复删除某标签时误清其它资源标签的潜在缺陷
- **首页搜索入口**：首页顶栏常驻"搜索会话和产物…"搜索框（点击即开全局搜索对话框，旁附 ⌘K 提示）——此前该能力仅有 ⌘K 快捷键可达
- **语言"跟随系统"偏好**：语言选择新增"跟随系统"且为默认值（原为仅首次启动探测后锁死）；系统/浏览器语言变更实时生效（zh/zh-Hant/en/ja/ko/fr/de/es/ru 9 语言选择器同步）
- **托盘菜单本地化**：macOS/Windows 托盘菜单与工具提示按系统语言显示（9 语言标签表，随 tray 创建生效）
- **翻译防退化门禁**：新增 `npm run i18n:coverage` 覆盖率报告与 pending 登记表机制——新增英文文案键必须当 PR 翻译进全部语言或显式登记，杜绝"新键只进中英、其余语言逐年烂尾"；本轮 9 语言新增 2 枚界面键（搜索占位、跟随系统）并同步翻译
- **第二波硬编码文案键控（第一批）**：上线 `npm run i18n:hardcoded-audit` 审计工具（扫描绕过 t() 的用户可见英文），首批键控 9 处高可见文案（数据迁移"当前（隐藏）"、审查中、连接器导出/导入的 传输方式/凭据/拖放校验、任务详情"远程工作目录"、引导页 Notebook 运行时、语言设置说明等）并九语言同步翻译；字典增至每语言 1,963 键
- **应用原生菜单本地化**：启动即安装随系统语言的九语言应用菜单（文件/编辑/显示/窗口 + macOS 应用菜单，role 保留原生快捷键），不再显示 Electron 默认英文菜单
- **凭据页三段式对齐**：设置→凭据按 服务（供应商卡片+密钥状态）/ 连接器凭据（空态+新建/管理）/ 自定义（说明文案）三区组织，复用既有数据流与译文
- **网络栏代理设置**：设置→网络新增**代理**区块——默认**跟随系统**（子进程使用操作系统当前代理设置），可切换**手动配置**并指定 类型（HTTP/HTTPS/SOCKS5）+ 服务器 + 端口 + 免代理列表（逗号分隔；localhost/127.0.0.1 始终直连）；作用于 Notebook/REPL/Bash 子进程（内核与 Shell 的 HTTP(S)/ALL_PROXY/NO_PROXY 环境注入，egress 出口白名单开启时以过滤代理优先、手动代理让位），重启后保持生效；网络出口白名单卡片同步更名对齐为"笔记本网络访问"（含"已允许 N 个自定义域名"计数摘要，说明文案改写为参考心智模型）；新增 23 枚界面键九语言同步（字典每语言 1,994 键）
- **模型栏场景模型分组**：设置→模型新增**场景模型**区（主模型与视觉模型之间）——会话详情 / 子智能体 / 审查 三行各自可选默认模型（默认**与主模型相同**，可单独指定任意已配置供应商的模型，选择器与视觉模型同款分组样式）；存储为 `scenarioModels` 设置映射（含 sanitize 门禁：仅已知场景 id + 非空 provider/model，非法载荷回退继承主模型）；如实标注框架边界——当智能体框架经 ACP 运行时，子智能体与审查的逐会话模型下发受框架限制（ACP 会话创建无 per-session model 字段），届时回退主模型（对应供应商模型/推理强度联动待框架层支持后接线）；新增 7 枚界面键九语言同步（字典每语言 2,001 键）
- **七语言全量翻译至 100%**：zh-Hant/ja/ko/fr/de/es/ru 字典由 6–7% 覆盖补齐至**每语言 1,954 键全量**（约 1.28 万条译文）——含拼接片段键、`{占位符}` 与 `<code>` 标签保真、嵌套词（其/its）按语言语法处理；配套质量门禁同步上线：zh-Hant 简体字残留扫描、跨语言"值=英文"白名单（同形词/借词/占位符）、pending 登记表全部清空——切换任意语言不再大面积回退英文
- **首页项目行文件类型徽标（T5 首项）**：每个项目行在名称右侧显示该项目 Files 目录中最近产物的小型类型 chip（MD/DOCX/CSV/PNG/PDB…，至多 4 个、按新近去重、窄屏隐藏）——对齐参考产品 20.06.15 的信息密度；数据来自项目文件索引的单次轻量读取，web 无桥接或空目录时自动不渲染；无新增文案键
- **专才市场排序控件（T5b）**：市场列表新增排序下拉——**最新版本**（semver 逐段降序，同一来源内等价"最近更新"，默认）/ **名称**（忽略大小写升序），等值保持原快照顺序；说明：市场快照不含 release 日期（来源按 git ref 解析版本），"按时间最近更新"需新增 release 日期采集链路，属后续专项；排序逻辑为无依赖纯函数并带 4 条单测；新增 3 枚界面键九语言同步（字典每语言 2,004 键）
- **多语言残留清理批次（2026-09-04）**：
  - 首页与开机标牌：**"Beta" 改为软件版本号**（v1.37.0，随更新信息显示；版本号本身无需翻译）
  - **相对时间本地化**：列表中的紧凑相对时间随界面语言显示本地单位（中文"2周"而非"2w"；zh-Hant/ja/ko/fr/de/es/ru 各有单位表，en 保持缩写；i18n provider 切换语言即同步）
  - **凭据页三段式文案键控**：服务 / 连接器凭据（设备级共享凭据说明 + "暂无连接器凭据。"空态）/ 自定义（"凭据保留在各自配置中…"说明）——此前整段硬编码英文，现 5 枚新键九语言
  - **记忆内置分类本地化**："About you" 在用户未改名时按界面语言显示（中文"关于你"等）；用户改名后以自定义名为准
  - **视觉模型空态**：无支持图像输入的供应商时给出说明文案（"当前没有支持图像输入的模型可选…"）而非空无一物
  - **远程控制面板残留键控**：canManage 提示（含配对管理版）、App/浏览器两种访问的引导段、步骤 2/3、"配对请求"标题、"拒绝"、请求时间（"{time}" 插值）；删除跟随中文整句后的重复英文尾巴（"and does not include…"为键值重复拼接）
  - 本批新增 16 枚界面键九语言同步（字典每语言 2,020 键）
- **对话工作区残留键控（2026-09-04，第二批）**：
  - **Usage 用量弹层**：Input/Cache read/Cache write/Cache/Output 段标签、轮次数（"1 轮/3 turns" 按语言）、智能体/模型前缀、"此响应的词元用量"等无障碍文案全部键控（原硬编码英文）；aria 总计句保持 {total} 插值
  - **消息时间本地化**：气泡时间与悬浮完整时间由界面语言驱动（中文"8月19日 1:21"而非 en-US "Aug 19"），新增 ui-locale 注册表随 i18n provider 同步
  - **输入框提示本地化**："/ 技能 · @ 文件 · # 会话 · ⌘K 搜索 · ↑↓ 历史"（en 保持原样，视觉基线不受影响）
  - 本批新增 18 枚界面键九语言同步（字典每语言 2,037 键）
- **第四批组 2（运行环境/技能详情）**：App 托管环境标题、禁用对话框（标题/两条影响说明含 {active}{running}{idle} 插值/停止运行/当前工作结束后禁用/Cancel）、技能详情 Details 标题与 Author/License 键控；禁用影响文案经 zh 机翻腔门禁修正（该会话→此会话）；9 键 × 9 语言（字典每语言 2,111 键）；键控同时核验交互闭环：Stop/Disable 按钮分别绑定 confirmForceStop(强制 abort 并记 cancelled)/confirmDisable(排空后关内核)
- **第四批组 3a（对话区/生命周期）**：运行失败区"报告错误"按钮（aria 已有键）、拖放附加浮层提示、LifecycleToast View/关闭 aria 键控；4 键 × 9 语言（字典每语言 2,115 键）；审查：Report error→openReportDialog 对话框入口存在、Toast onView/onDismiss + 6s 自动关均接真回调
- **第四批组 3b（审查者/验证清单/折叠时间线）**：Go to transcript（ReviewerCard+清单共用）、Re-running/Re-run review、Mark addressed、Verification checklist、"{open} 项待处理，共 {total} 项"、折叠上下文/预览/折叠计数（{n} fold{s}）、折叠原因标签（自动/手动/溢出恢复，原 REASON_LABELS 英文硬映射）、时间随界面语言（toLocaleString(getUiLocale)）；12 键 × 9 语言（字典每语言 2,127 键）；审查：清单行 Mark addressed/Reopen 绑定 onMarkAddressed/onReopen、Go to transcript 均接 GoToTranscriptIntent 链路
- **第四批组 4a（模型选择器/预览/写入审计）**：无可用模型 aria、"打开设置"菜单项、审查视图 tablist aria（两处）、写入审计标题与"N 个文件变更—新建/修改/删除"摘要（{n}/{s}/{created}… 插值）；5 键 × 9 语言（字典每语言 2,132 键）；审查：无模型按钮 openSettings 打开设置对话框链路、tab 切换三视图均有对应面板组件
- **开机加载文案键控（组 1b）**：设置加载门"正在加载设置…"、重试、会话恢复门"正在加载已保存的会话…"随界面语言（语言在首帧即从本地存储解析，不受设置加载影响）；4 键 × 9 语言（字典每语言 2,102 键）
- **第四批组 1（JobDetail/权限面板）**：任务详情弹层 Job ID/提供商/状态/运行时标签、权限面板“全部撤销”按钮+aria、“默认使用完整访问权限？”对话框标题/确认钮/取消键控（9 键 × 9 语言，字典每语言 2,098 键）
- **远程控制/全局搜索/Web 搜索行残留键控（2026-09-04，第三批，一次到位）**：
  - **主进程错误消息全量本地化**：`src/main/remote-access/*` 抛出的 33 条英文错误（Remote.It 服务创建/切换、端点、云配置、本地服务停止、"应用未安装…请重新检测"等）经 `remote-error-catalog` 精确映射回字典键，界面按当前语言显示（含 zh-Hant/ja/ko/fr/de/es/ru 全文翻译）
  - **远程控制面板补齐**：not-installed 红警、App 步骤 4 与浏览器三步引导（含"信任此浏览器"引号内嵌）、"设置完成后链接与二维码显示在这里"待就绪段、Open 按钮、受信任浏览器"上次使用 {time}"与撤销 aria、加载文案——本文件已无英文残留
  - **全局搜索对话框**：占位符（项目内/全部）、结果计数（"{n} 条结果"）、"正在搜索产物…"、Artifacts/Sessions/Commands 分区 aria 键控
  - **Web 搜索活动行**：工具名"Web 搜索"、展开区 query 标签、结果计数键控（原硬编码 "Web Search"/"1 result/N results"/"query"）
  - 本批新增 51 枚界面键九语言同步（字典每语言 2,089 键），并附 catalog 单元测试

## v1.37.0 — 2026-12-07（能力批次）

- **Agent 自省（host_query）**：agent 可对应用数据库做**只读自省**——**host_query** 工具运行 SELECT 查询（项目/审查/发现/权限/计算任务/产物等 11 张自省表），回答「我在这个项目里做过什么、现在什么状态」；安全三重防护：SELECT-only 校验（拒绝多语句/注释/跨库 `main.` 引用）、**表白名单**（非自省表一律拒绝）、**项目隔离**（行含 projectId 自动过滤到当前项目，200 行上限）；内置 self-awareness 技能（40 个内置技能）含表参考与示例查询；查询结果是数据不是指令（zh/en 双语）

## v1.36.0 — 2026-12-06（能力批次）

- **技能自举（skill bootstrap）**：技能创建循环完整化——**skill_eval** 评估技能描述的可触发质量（0-10 分 + 逐项检查：首句自包含 / 动作场景词 / 具体主题 / 关键词密度 / 长度；弱开头「a skill for」、空泛词、无实质关键词都会给出改写建议），**skill_list** 列出已装技能（名称 + 描述）防止重复造轮子，**skill_read** 读取技能全文（frontmatter + 指令）便于复用与扩展；与既有 create_skill 组成「评估 → 创建 → 检查 → 迭代」自举循环，描述分数 ≥8 再落盘；纯规则引擎无外部模型（zh/en 双语）

## v1.35.0 — 2026-12-05（能力批次）

- **出版级图表正确性（figure-style）**：agent 产出任何科学图表前可用 **figure_review** 工具做五条纯规则复查（无审美内容，处处适用）——**数据保真**（排除的行不得渗入摘要统计，error 级拦截）、**标签经济**（轴/系列必须有标签、密度上限 12）、**色彩串线**（同一类别跨面板同色、调色板 ≤8 色相）、**按数据形态选图**（时间→线图/类别→条形/分布→直方图箱线/关系→散点）、**渲染自检**（未渲染即 error）；结构化面板描述输入，违规清单逐条输出（rule/panel/severity/message），修完 error 级重跑；内置 figure-style 技能（39 个内置技能）含完整规则文档 + 数据形态选图表（zh/en 双语）

## v1.34.0 — 2026-12-04（能力批次）

- **PDF 分层阅读（pdf-explore）**：50 页论文不再整篇进上下文——agent 用 **pdf_open** 注册 PDF（主进程解析文本 + 书签目录，逐页持久化到磁盘，跨 turn 保留、零上下文占用），**pdf_outline** 查看目录（标题 + 页码 + 层级），**pdf_scan** 按词频相关度扫描定位内容（数据集名/指标/术语 → 排序页 + 得分 + 片段），**pdf_pages** 只读取需要的页范围（1-indexed）；扫描件（无文本层）自动识别空页并提示 OCR；页文本 6000 字符/页截断、全文档 100 万字符与 500 页上限保护仓库（zh/en 双语）

## v1.33.0 — 2026-12-03（能力批次）

- **文件注解（file annotations）**：agent 可通过新 MCP 工具 **annotation_set** 给项目文件打带标签的注解（todo/question/important/note/review + 简短备注，路径相对项目根，同标签重复设置自动替换），**annotation_list** 查看（可按文件过滤），**annotation_remove** 删除；注解锚定内容版本（可选 file_sha256，检测内容漂移）；路径安全校验拒绝绝对路径与 .. 逃逸；文件面板每行新增注解按钮（hover 显示），弹窗内可视化查看/添加/删除（zh/en 双语）
- **模型 runbook 扩充**：新增 **生物模型本地托管**（managed-bio-endpoints）——通用托管模式（FastAPI 包装 + 幂等启停 + 就绪路由 + 权重缓存挂载）+ fair-esm2 轻量示例与 esmfold2 GPU 示例；连同 llamacpp-local-server 共 2 个本地托管 runbook，38 个内置技能

## v1.32.0 — 2026-12-02（能力批次）

- **本地模型服务（managed endpoints）**：agent 可通过新 MCP 工具 **endpoint_register** 注册本地模型服务（名称 + 回环地址 + 模型技能 + 幂等启停脚本 + 就绪路由），**endpoint_free_port** 从托管区间（20000-29999）分配端口，**endpoint_start/endpoint_stop** 掌控生命周期，**endpoint_status/endpoint_list** 查看状态；主进程守护生命周期——首次注册的脚本集需在设置 → 本地模型面板批准（sha256 白名单，字节相同静默通过，任何改动重新审批），启动后轮询就绪路由，就绪才放行调用，连续失败进入 failed 并记录 last_error；凭据值经环境变量注入启动脚本（agent 端永不见明文）；内置 llama.cpp 本地推理 runbook（zh/en 双语）
- **设置 → 本地模型面板**：注册表单（名称/回环地址/模型技能/凭据/启停脚本/就绪路由）+ 生命周期卡片（运行中/启动中/失败/已停止 + 启停/删除）（zh/en 双语）

## v1.31.0 — 2026-12-01（能力批次）

- **定时任务（scheduled tasks）**：agent 可通过新 MCP 工具 **routine_configure** 编排周期任务（自包含指令 + 5–1440 分钟间隔），**routine_status** 查看计划与执行结果，**routine_cancel** 取消；主进程 30 秒节拍调度器到点后把指令派发为全新任务运行（沿用任务运行器，不引入新执行引擎），连续 3 次失败自动暂停（reason=stuck）防止失效任务空转；设置 → 定时任务面板提供可视化新建/暂停/恢复/删除（zh/en 双语）
- **数据源许可门控（license gate）**：connector 工具可声明 `noncommercialOnly`——CADD 变异危害评分（非商业用途限制）已标记；在「使用目的」为商业用途时调用受限工具会 fail-closed 拒绝并给出明确指引（切换到非商业即可启用），合规边界从"文档说明"升级为"调用期强制"（zh/en 双语）

## v1.30.0 — 2026-11-28（能力批次）

- **任务并发上限（task concurrency ceiling）**：任务运行器新增全局并发上限——同时运行的任务数可配置约束，超限时拒绝新任务并提示「Too many concurrent task runs」；运行完成自动释放配额；每会话单运行串行保持不变，此上限跨会话生效（zh/en 双语）
- **Agent 档案生态确认**：个人专才档案（自定义名称/描述/系统提示/图标/能力模式/技能绑定）已支持全生命周期（创建/编辑/启用/复制/导出/会话绑定）——本次审计确认端到端可用，无需改动；SSH 密码认证（凭据面板 + SSH_ASKPASS 注入）与主机级并发上限亦已完备

## v1.29.0 — 2026-11-26（能力批次）

- **Deny-first 网络防护（deny-first egress）**：内置外泄域名黑名单（云元数据端点——AWS/GCP/Alibaba 凭据外泄目标），**deny 永远优先于 allow**——即使网络白名单关闭或用户手动添加了黑名单域名，这些端点也一律 403；HTTP 与 HTTPS CONNECT 隧道双路径生效（zh/en 双语）
- **写入审计（write audit）**：notebook 工具新增「写入审计」面板——聚合每次代码/Shell 执行产生的文件变更（路径/变更类型/大小/时间/所属运行），支持按 创建/修改/删除 筛选；工作文件观察器新增变更类型检测（含此前未捕获的**删除**事件），让"agent 写了什么"一目了然

## v1.28.0 — 2026-11-24（能力批次）

- **记忆溯源（memory provenance）**：记忆笔记支持来源证据——agent 保存笔记时可附带 `evidence`（来源说明，如来自哪个会话/产物），记忆面板显示「来源」标签；被取代的笔记标记「已取代」且**不再被自动回忆**（保留可见可编辑，避免上下文矛盾）；`memory_save_note` MCP 工具新增可选 `evidence` 参数，持久化层保留溯源字段（zh/en 双语）
- **Artifact 环境证据确认**：产物证据体系已含完整环境快照（`ArtifactVersionEnvironmentEvidence`：包清单/版本/生态/加载状态 + code/execution/environment 三视图）——本次审计确认端到端可用，无需改动

## v1.27.0 — 2026-11-22（能力批次）

- **科学数据补齐（science data gap closure）**：connector 目录新增 3 个数据源——**CADD**（变异危害评分，PHRED/raw，单变异与位点查询，GRCh37/38 多版本）、**DepMap**（癌细胞系依赖性评分 Chronos，细胞系搜索 + 基因依赖性排行）、**PanglaoDB 规范标记基因**（离线内置，单细胞注释：细胞类型→标记基因、基因→细胞类型、类型清单，免疫/基质/上皮/器官特异全覆盖）；「变异」「表达」「癌症模型」connector 的目录元数据同步更新（zh/en 双语）

## v1.26.0 — 2026-11-20（能力批次）

- **可检索长上下文（searchable long context）**：上下文压缩不再丢弃原文——每次压缩时被折叠的对话窗口持久化为不可变 chunk，agent 可通过新 MCP 工具 **summary_query** 检索折叠内容的逐字细节（精确错误信息/文件路径/数值/决策），**boundary** 工具让 agent 标记任务边界（折叠落在任务之间）；审查工具新增「Folded context」标签页展示折叠时间线（时间/原因/边界标签/token 数，可展开摘要与原文预览）；能力开关随会话 capability 自动注入系统提示（zh/en 双语）

## v1.25.0 — 2026-11-18（能力批次）

- **核验清单（verification checklist）**：审查工具新增「清单」视图——跨 review 聚合会话内全部 warn/fail 声明（claim），每条显示最新判定（pass/warn/fail）/证据/重标记次数/被评估次数，支持一键「标记已解决 / 重新打开」；会话无审查记录时自动展示清单入口（zh/en 双语）

## v1.24.0 — 2026-11-16（能力批次）

- **子任务模型覆盖（sub-agent model override）**：任务/子代理会话创建支持指定模型——`StartTaskRunRequest.modelId` 经任务运行器透传至会话创建，会话配置器优先使用请求的模型而非后端当前模型（delegate/reviewer 子任务可独立选择模型）；向后兼容，未指定时沿用现有行为

## v1.23.0 — 2026-11-14（能力批次）

- **Usage 按运行归属（per-run usage）**：设置 → 使用量 新增「按运行查看」区块——每次运行（root 会话帧）显示实际消耗 token，委托子运行（sub-agent/delegate/reviewer）的用量自动归并到所属主运行并标注子运行数，帮助定位高消耗任务；无会话图的旧会话自动合成单条运行（zh/en 双语）

## v1.22.0 — 2026-11-12（能力批次）

- **更新下载加固（update hardening）**：更新清单（manifest）解析新增下载来源白名单——仅允许官方域名与 GitHub Releases 资产域（https 强制），被篡改/非白名单 URL 在解析阶段即拒绝；manifest 拉取新增 10 秒超时，防止更新流程被挂起的请求阻塞

## v1.21.0 — 2026-11-10（能力批次）

- **凭据引导恢复（guided recovery）**：凭据验证结果新增失败分类（auth/network/format/unknown）并按类显示针对性恢复提示（密钥失效重输/网络不可达/格式错误等）；编辑凭据保存后自动重新探测——通过即关闭，失败则停留在编辑框内给出恢复指引，闭环修复（zh/en 双语）

## v1.20.0 — 2026-11-08（能力批次）

- **许可使用声明（use intent）**：设置 → 通用 新增「使用目的」——商业用途（默认）/非商业用途；启用声明为限制性许可（如 CC-BY-NC）的技能时，商业模式下先弹出许可确认对话框，确认后才启用（zh/en 双语）

## v1.19.0 — 2026-11-05（能力批次）

- **第三方许可证面板**：设置 → 关于 新增「第三方许可证」入口——列出应用依赖的开源软件许可证摘要（取自安装依赖树，名称/版本/许可证文本），便于合规审查；主进程收集器 + 新 IPC 通道 + 对话框 UI（zh/en 双语）

## v1.18.0 — 2026-11-03（UI 交互批次）

- **会话未读续接引导**：侧栏会话列表显示「新」徽标——会话在你上次阅读后有新活动时出现（基于上次打开时间，localStorage 持久化）；打开会话自动标记已读；活动会话永不显示未读
- **模型选择器不可用原因本地化**：灰置模型/组的完整原因文案（Codex 桥不支持等）走 i18n；i18n 引擎新增 `{变量}` 插值支持（t(key, vars)，向后兼容）
- **Usage 面板确认对齐**：token 用量时间范围控件（24h/7 天/30 天切换、响应式短标签）确认与参考产品一致

## v1.17.0 — 2026-10-31（中文翻译覆盖度审计批次）

- **硬编码英文全量补齐**：全库审计发现 234 处未走 i18n 的用户可见文案（文本节点/placeholder/aria-label/title 全类型），逐面板移入双语字典（新增约 190 个键，en/zh 同步），覆盖设置/工作区/预览/引导流程/组件对话框等全部界面
- **术语一致性修复**：全界面「专家」→「专才」统一（术语表 specialist=专才，34 处误用修正，含面板/导入/搜索/编辑器等全部场景）
- **机翻腔抽查**：全量扫描被动语态直译/英文句式残留，现有文案无严重问题；术语表回填 4 条（权限模式/配置预览/配置诊断/仓库结果）

## v1.16.0 — 2026-10-27（P3 生态与提供商 · 4.3）

- **MCP 标准配置导入导出**：导入兼容**宿主文件多形态**——Claude Desktop 的 `claude_desktop_config.json`（顶层 `mcpServers` 嵌套 + 无关键自动忽略）、Cursor 的 `mcp.json`、裸 `mcpServers.json` 均可直接导入；导出保持标准 `mcpServers` 形状且凭证以 `${NAME}` 占位符脱敏，可安全分享
- **全量收尾**：Phase 3/4 全量回归（测试/typecheck/build/打包验证）、术语表终审回填（模型网关/目录标签/图片区域注解/MCP 配置等 8 条）

## v1.15.0 — 2026-10-22（P3 生态与提供商 · 4.2）

- **Grok 三协议全通**：新增本地 Messages→Responses 翻译桥——Claude Code 的 Anthropic 协议请求经本地代理翻译为 xAI 原生 Responses 请求（消息/工具/流式事件双向映射），Grok 订阅（OAuth）与 xAI API Key 均可驱动 Claude Code，至此 Grok 在 Claude Code / OpenCode / Codex 三个框架下全部可用
- **DeepSeek V4 Pro Responses 直连**：V4 Pro GA（2026-08-13）原生 Responses API——`deepseek-v4-pro`（含 `[1m]` 变体）不再走 Chat Completions 桥，与 V4 Flash 一样直连 `/v1/responses`
- **GitHub 认证技能导入**：技能导入/预览/仓库扫描/搜索自动携带统一凭据中的 GitHub token（仅注入 github.com 与 api.github.com，限流提升 + 私有仓库导入），凭据验证/保存/替换复用统一凭据面板

## v1.14.0 — 2026-10-19（P3 生态与提供商 · 4.1）

- **模型目录扩充**：新增 **OpenCode Zen** 精选网关（GPT-5.x / Claude / Gemini / Grok / 免费模型等 23 个精选条目，含原生 Responses 端点路由）与 **OpenCode Go** 配额计划目录（MiniMax / Kimi / GLM-5.3 / DeepSeek V4 / Qwen3.8 / Hy3 等 21 个条目）
- **腾讯 TokenHub 提供商**：腾讯大模型服务平台（原混元 / DeepSeek API 入口迁移目标）——DeepSeek V4 Pro/Flash、Hy3、GLM-5.3、Kimi-K3、MiniMax-M3 等 13 个模型，Hy3 与 DeepSeek V4 家族支持原生 Responses 路由
- **混合协议端点路由**：Zen 的 GPT-5.x 家族走 `/zen/v1/responses` 原生端点、其余模型走 Chat Completions——per-model 端点元数据驱动，live 模型刷新同样覆盖

## v1.13.0 — 2026-10-14（P2 体验打磨 · 3.4）

- **注解图片版补全**：agent 生成的图片支持框选区域注解——点「框选图片区域」进入框选模式，拖拽圈定区域；选中后可**放大 2 倍精调边界**（四角手柄微调）、**重选**或**取消**；确认后生成图片区域注解卡（含归一化坐标），随下一条消息作为引用上下文发送给 agent
- **注解卡升级**：图片区域注解显示图标徽标与坐标信息；消息内注解 chips 区分文本注解与图片区域注解
- **持久化兼容**：注解契约扩展为 text / image 判别联合，图片注解经严格校验（区域合法性与边界钳制）后随会话持久化，非法数据 fail-closed 丢弃

## v1.12.0 — 2026-10-12（P2 体验打磨 · 3.3）

- **跨资源目录标签与收藏**：技能、连接器、专才三大目录统一标签模型——每项可打标签（≤8 个，去重、规范化）、可收藏；任一目录行内直接添加/移除标签，星标一键收藏，localStorage 持久化
- **目录过滤条**：全部 / 收藏 / 标签 chips 三段式过滤（标签与收藏 AND 组合，多标签叠加）；chips 基于当前视图自动汇集已有标签，随过滤实时收窄
- **技能面板重设计**：范围分段（全部 / 主 agent / 专才技能，按专才有效技能绑定自动归组，专才徽章标注）；紧凑工具栏 + 批量操作模式——全选、批量启用/禁用、批量删除（内置技能保护跳过并计数提示），`Promise.allSettled` 逐项容错
- **连接器与专才面板同步升级**：收藏 + 标签 + 过滤条全线接入，与技能目录一致

## v1.11.0 — 2026-10-10（P2 体验打磨 · 3.2）

- **run-marks 轮次导航轨**：长对话（≥8 轮）右侧竖向圆点导航——每个用户轮次一个标记，随内容比例分布；当前阅读位置高亮；点击任意圆点平滑跳转到该轮起点；千轮长对话可快速定位，短对话自动隐藏
- **会话导出按轮次**：导出对话框支持 Markdown/PDF 双格式 + 三态轮次选择（全部轮次 / 指定范围 from–to / 单轮）；轮次超界自动收敛到实际轮数；范围非法时禁用导出并提示；保留基础全量导出路径不变

## v1.10.0 — 2026-10-08（P2 体验打磨 · Phase 3 之始）

- **会话 hover 预览卡**：侧栏会话行悬停/聚焦显示即时预览卡——完整标题（长标题可滚动）、描述全文；右侧优先、放不下自动翻到左侧、底部钳制不溢出窗口；快速扫过列表不闪卡，滚动/切换会话/打开菜单即收起
- **应用内沙箱源预览**（延续）：agent 回复链接悬停显示来源卡（标题 + URL），点击在右侧沙箱 iframe 打开——HTTPS-only、无同源/无顶层导航，双闸防护在位
- 修复：HTTP-hosted skill MCP 传输补全 create_skill 接线（typecheck 恢复全绿）

## v1.7.0 — 2026-08-29

- **会话式技能创建**：会话中的 agent 可从自然语言起草技能（create_skill 工具）——名称/描述/步骤说明/引用，应用侧校验（名称格式、大小上限、引用数量）并落盘为 `SKILL.md`；agent 不直接写技能文件，持久化由应用全权负责

## v1.6.0 — 2026-08-29

- **SSH 密码认证**：远程计算主机支持密码认证（绑定凭据面板凭据 ID）——密码仅在连接时解密、经 SSH_ASKPASS 注入，绝不落盘到计算配置、绝不出现在命令行；密钥认证与 ssh-agent 路径保持不变

## v1.5.0 — 2026-08-29

- **@path 本地文件夹授权**：授权智能体读取本地目录——跨盘浏览（本地卷/路径栏）、一键授权、已授权列表管理与随时撤销；授权跨会话持久化，撤销立即失效
- 链接文件夹内的文件引用严格限定在授权根目录内（目录穿越逃逸 fail-closed）；敏感路径（密钥目录等）拒绝授权

## v1.4.0 — 2026-08-29

- **消息中心接入事件源**：收件箱开始真实记录 agent 事件——回合完成（task.completed）/回合失败（task.failed）/等待授权（authorization.required，待处理动作），桌面与 Web 共享同一后端收件箱；按（类型, 会话, 来源）去重，重试不产生重复卡片
- 收件箱标题以稳定标识存储，界面按语言映射（任务完成/任务失败/等待授权）

## v1.3.0 — 2026-08-29

- **文本注解进会话**：在转录、工具活动或文件预览中选中文本 → 「添加为注解」→ 选区作为上下文卡片随下一条消息发送给 agent（携带来源、跨重启保留、支持 edit-and-resend；每消息最多 8 张、单张 8000 字符有界）
- 注解以显式引用块（`<annotation source=…>`）注入 agent 提示，agent 视为权威用户上下文

## v1.2.0 — 2026-08-29

- **安全加固批次**：
  - 渲染器 Chromium 权限请求默认拒绝（任何 web 权限请求一律拒绝，缩小受损渲染代码的攻击面）
  - 远程计算作业输出落盘前敏感凭据脱敏（sk-/AWS/GitHub/Bearer/token= 等模式整体掩码，作业 tail 不再保留可用密钥）
  - 连接器卡死请求快速失败（30s 超时后不再重试，立即报带截止时间的错误；瞬时网络错误仍按退避重试）
  - agent 事件流有界接纳（16ms 窗口内限流高频事件，stop/error/artifact 等关键事件始终放行，万级事件回合不再冻结界面）

## v1.1.0 — 2026-08-29

- **结构化澄清工作流**：agent 可通过 ACP elicitation 发起多问题澄清——单选/多选/文本/数字/布尔字段、必填校验、跳过或提交；回答聚合后注入 agent 上下文（桌面与 Web 全链路）

## v1.0.0「浑仪」— 2026-08-29

- **版本里程碑**：独立版本号体系正式启用（v0.x → v1.0.0），与既有命名体系彻底解耦；
  本地标签空间完成治理（外部标签移入独立命名空间，git describe 恢复正确基线）

## v0.17.2 — 2026-08-28

- **Serverless GPU 与模型端点（Modal + NVIDIA NIM）**：设置 → 计算新增「Serverless GPU 与模型端点」区块——可配置 Modal serverless-GPU 端点（作业经 modal CLI 在 GPU 容器中执行）与 NVIDIA NIM 推理端点（OpenAI 兼容模型调用，可指定 Base URL 与模型名）；每个端点绑定「凭据」面板中的 Modal token / NVIDIA API key（密钥解密后注入，绝不落盘到计算配置）；作业调度器按 provider id 前缀自动路由（`modal:` / `nvidia_nim:`），提交到外部端点的作业与 SSH 作业走同一状态追踪

## v0.17.1 — 2026-08-28

- **网络出口白名单（Network egress allowlist）**：设置 → 网络新增「网络出口白名单」——总开关开启后，Notebook / REPL / Shell 子进程经本地过滤代理访问网络，仅放行 6 组科学域（文献与仓储 / 基因组与变异 / 结构与化学 / 临床试验与药物 / 生物信息工具 / 代码与软件包仓库）+ 用户自定义域名，其余一律拒绝；默认关闭（不改变现有行为），域名按后缀匹配（子域名自动涵盖）；改动即时生效于后续启动的子进程

## v0.17.0 — 2026-08-28

- **统一凭据管理（Credentials 设置页）**：为 AWS / GitHub / Google Cloud / Azure / Modal / NVIDIA / OpenAlex / 文献访问 8 类科学服务及自定义凭据提供统一存储——密钥经系统钥匙串（safeStorage）加密落盘，渲染层只见掩码提示，明文永不离本机；支持新增/编辑（不重输密钥可保留旧值）/删除（确认对话框）与连接测试（GitHub 走真实 API 校验）；设置侧栏新增「凭据」入口

## v0.16.4 — 2026-08-28

- **agent 自动保存记忆（memory_save_note）**：会话中的 agent 可通过专用 MCP 工具把符合分类保存指引的信息写入用户记忆——`purescience-memory` 服务端 + 会话级令牌 RPC 落库，主进程单写者持久化；recall 提示块附带各分类保存指引，形成「设置分类 → agent 按指引保存 → 后续会话 recall 注入」的完整闭环

## v0.16.3 — 2026-08-28

- **记忆分类可改名/删除**：分类行 hover 提供铅笔（行内改名，Enter 提交/Esc 取消）与垃圾桶（确认对话框后删除分类及其笔记）；内置「About you」可改名不可删除（归一化会自动重建）

## v0.16.2 — 2026-08-28

- **记忆 recall 注入**：记忆开启时，把各分类笔记渲染为有界提示块（每分类 20 条、每条 600 字、总预算 4000 字、超限整块丢弃）注入 agent 会话系统提示——三个框架（claude-code / opencode / codex）全部生效，记忆从「仅存储」变为「真正参与会话」

## v0.16.1 — 2026-08-28

- **记忆面板输入对齐**：新增内联「添加笔记」输入框（Enter 保存、提交后自动聚焦），笔记生来即有文字；新建分类改为完整表单（名称 + 保存时机指引 + Auto-recall 开关 + N/10 计数，Create 需名称非空）；数据清洗器允许空文本笔记（清空重打不再被丢弃）

## v0.16.0 — 2026-08-28

- **Memory 设置页（分类笔记 + 总开关 + Clear all）**：全链路新建——shared schema → 主进程 SQLite 存储 + IPC → 渲染层 store → MemoryPanel + 设置导航 + 双语文案；支持内置「About you」分类与自定义分类、每分类笔记增删改、主开关（关闭后笔记保留仍可编辑）与一键清空

## v0.15.0 — 2026-08-27

- **审阅者外部来源验证 + 跨窗口追踪**：审阅者可抓取被引用 URL 的纯文本摘要（走应用网络栈、遵循系统代理）并与被审回合的声明比对；跨窗口追踪同一证据链
- **分类导航中文深化**：设置面板分类标题与按钮中文化（General/Model/Connectors 等区块）
- **多源并行文献综述**：`literature_review_search` 聚合工具落地——提取 arXiv / OpenAlex 搜索可复用核心，单次调用并行查询多源（单源失败不拖垮整体，结果带 source 标记）

## v0.14.4 — 2026-08-22

- **更新对话框本地化**：version.json 支持双语 release notes（zh/en），更新说明跟随界面语言
- **权限拒绝强化**：用户拒绝后，agent 明确被告知无授权且不得在本回合重试或绕行（连接器/远程命令/下载审批）
- **长粘贴文本附件化**：composer 粘贴超过 10000 字符或 300 行的纯文本自动转为附件卡片（原文完整保留），不再淹没草稿

## v0.14.3 — 2026-08-22

- **xAI Grok OAuth 订阅**：新增「xAI Grok 订阅」提供商——SuperGrok / X Premium+ 账号设备码登录（RFC 8628，auth.x.ai），无需 API key；token 加密存储 + 自动刷新；OpenCode（Chat Completions）与 Codex（Responses）双路由直达 api.x.ai/v1；默认 grok-4.6
- 注：xAI 后端对 OAuth API 有订阅档位限制（部分档位登录后 403），官方 xAI API key 路径仍为可靠兜底

## v0.14.2 — 2026-08-22

- **摘要优先启动**：会话列表启动不再逐个解析完整会话 JSON——每次保存时附带写入轻量摘要缓存（指纹校验 size+mtime），启动列表直接从摘要读取；会话文件变更时自动失效回退全文。会话多时启动明显提速

## v0.14.1 — 2026-08-22

- **受管专才市场（二阶段）**：专才列表新增「市场」筛选分组；市场安装的专才在编辑器中显示受管提示（发布者内容由市场管理，自由修改请先复制本地副本）；更新可用性沿用 SemVer 严格比较
- 编辑器「标识」区补全中文化（标识/预览/实时/图标）

## v0.14.0「神农」— 2026-08-22

> 神农：辨百草，识药性——市场生态从此有源可溯、有版可查。

- **受管专才市场（一阶段）**：市场安装的专才自动标记来源（marketplace origin），列表显示「市场安装 · v版本」来源标注；市场包与本地/导入包清晰区分，为只读发布者内容与更新校验铺路
- 列表操作菜单补全中文化（复制 / 导出 ZIP）

## v0.13.9 — 2026-08-22

- **Notebook 依赖过期标记上屏**：会话 Notebook 面板中，变量被后续运行重写的单元输出显示「已过期」徽标——旧结果一眼可见，不再误导

## v0.13.8 — 2026-08-22

- **Grok 4.6 模型**：xAI 官方目录加入 grok-4.6（1M 上下文）
- **Notebook 跨运行依赖追踪**：每次运行记录脚本写入的变量，后续运行重写同名变量时自动标记该运行输出「已过期」——科研结果不再被静默的旧状态误导
- **专才市场治理基础**：来源类型支持市场安装（origin: marketplace），为受管包生命周期铺路

## v0.13.7 — 2026-08-22

- **DeepSeek 视觉模型**：deepseek-v4-flash-vision-exp 加入官方目录（Responses 直连 + 多模态图片输入）
- **自定义模型 token 上限**：最大输入/最大输出 token 独立配置（表单 + 存储 + 后端透传）

## v0.13.6 — 2026-08-22

- **中文适配深度补全**：二次排查修复 40+ 处遗漏（新建项目对话框、诊断页、远程访问、消息中心、上下文窗口、权限/审查面板、专才/计算面板、消息状态与耗时等），含中英混合文案与 i18n 重复键清理

## v0.13.5 — 2026-08-22

- **中文适配全面补全**：修复 60+ 处遗漏的英文界面文案（工作区/设置页/对话框/菜单），新增 60+ 翻译键，覆盖状态、按钮、标签、占位符、提示

## v0.13.4 — 2026-08-22

- **叙事独立化**：全仓库统一为 PureScience 自己的术语与描述体系，代码注释、文档、用户文案与标识符采用完全原创的表达——以独立项目的身份对外叙事

## v0.13.3 — 2026-08-22

- **产物自动预览**：预览面板打开时，运行完成的最新产物自动呈现（Notebook 图表直出屏幕）
- **README 品牌独立**：版本徽章/公告/品牌章节更新，官网 purescience.work + 更新日记入口

## v0.13.2 — 2026-08-22

- **关于页资源区**：帮助中心（官网）、更新日记（GitHub Releases）、报告问题（Issues）一键直达

## v0.13.1 — 2026-08-22

- **Notebook 预览**：`.ipynb` 文件原生渲染（Markdown/代码单元 + 输出与错误高亮 + 单元统计）
- **自定义 MCP 连接瞬断自愈**：服务器进程退出或连接中断时自动重连并重试，不再报硬错误

## v0.13.0 「启明」— 2026-08-21

> 启明：我们以此为界，正式确立 PureScience 的独立品牌叙事。

- **品牌独立宣言**：更新日记启用自有代号体系；全部功能以原创开源方式自主开发与维护
- **官方专才市场正式上线**：ed25519 签名验证的市场协议、GitHub 源审查添加、
  发布详情/技能选择/安装溯源全流程；首个示例专才「示例研究员」随官方市场发布
- **国内网络适配**：市场元数据与安装包经 jsDelivr CDN 分发（国内直连可用），
  官方市场在国内网络下可直接访问
- **更新分发打通**：版本更新改为经 GitHub Releases 分发（`latest/download/version.json`），
  发布一条龙脚本 `scripts/publish-release.sh`（构建 → 哈希 → 清单 → 发布 → 自动验证）
- **科学计算环境自愈**：托管运行时离线恢复（复用本地完整环境 + 就绪标记），
  不再依赖不可达的下载源；常见科学库（numpy/pandas/scipy/scanpy 等）随环境就绪
- **运行稳定性修复**：
  - 项目打开白屏根治（界面组件缺少上下文提供者导致的渲染崩溃）
  - 智能体中断后「无法继续」修复（中断引发的内部诊断按正常取消处理，会话可继续）
  - 内置技能清单完整性修复（9 个技能文档重命名对齐）
- **品牌信息统一**：仓库归属、版本号、链接全部指向 PureScience 自有标识

## v0.12.x 「星火」— 2026-08

> 星火：功能与生态的播种期。

- **v0.12.0** 专才市场初版（签名验证协议、源管理、发布安装全链路）、
  更新系统接通 GitHub Releases
- **v0.12.1** 官方市场失败关闭语义修正（无签名密钥时优雅降级，市场保持可用）
- **v0.12.2** 官方市场启用（可信签名密钥上线，发布首个示例专才）
- **v0.12.3** 官方市场 CDN 化（jsDelivr 国内加速）；运行稳定性修复

## 更早版本（v0.11 及之前）

- 多智能体协同编排（任务拆解、并行执行、上下文洞察）
- BioNexus 生物医学技能包（17 个专业技能：变异解读、药物设计、单细胞分析等）
- 全界面中文适配（约 1300 个界面文案）
- 计划工件预览、视觉中继、消息分支、使用统计看板
- 连接器生态（文献检索、生物数据库 MCP 接入）
