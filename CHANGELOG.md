# PureScience 更新日记

PureScience 是一款面向科学研究的开源 AI 工作台：多智能体协同、科学计算环境、
专才市场与可审计产物，服务于分子生物学、基因组学、药物研发等科研场景。
我们坚持独立原创、开放共享——愿以开源之力，造福全人类。

## 版本代号

每一版都有一个取自中华科学文明的中文代号：

| 代号 | 含义                                         | 版本     |
| ---- | -------------------------------------------- | -------- |
| 浑仪 | 张衡浑天仪，窥天测地、度量万象——正式版里程碑 | v1.0.0   |
| 启明 | 启明星，长夜之后的第一缕光——品牌独立的起点   | v0.13.0  |
| 星火 | 星星之火，可以燎原——功能与生态的播种期       | v0.12.x  |
| 神农 | 尝百草，辨药性——生物医药方向的未来篇章       | （规划） |
| 天工 | 天工开物——工具与工程体系的未来篇章           | （规划） |
| 北斗 | 指路星辰——导航与检索体系的未来篇章           | （规划） |

## v1.70.0 — 2026-09-23（空态说得清为什么空，文案九语齐备）

**这一轮补的是「界面在什么时候说什么话」。** 上一版把键盘路修通，这一版把三类状态凑成套：**空**、**加载中**、**出错**——此前大量区域只有「有数据」和「没数据」两种长相，加载中与空态一模一样，于是「还没读到」被界面说成了「没有」。

**空态（11 处，8 改 3 归档）**：每一处都要求说清「为什么空」+ 给出「下一步点哪」。侧栏会话列表过去是空白，现在区分「这个项目还没有会话」与「筛选后没有匹配」；权限面板的空态原本是读屏专属文本，现在区分「一条都没记住」与「当前范围为空」并给清除筛选按钮；命令面板在浏览态下门控错误导致整屏空白，改为显式空态，且无结果时区分「筛选收窄」与「关键词未命中」；agent 框架面板、上下文面板（此前直接 `return null`）、记忆面板、存储用量条、溯源「执行」页（原本只有一句「无法确定」）逐处补上。三处经复核确认现有形态已是范式（技能上传、连接器导入的投放区、组学面板未选主机说明），零代码归档。

**加载态与错误态（27 处）**：四处 `Loading…` 进字典；分子渲染兜底、笔记本与产物加载失败、产物弹层三态、切换专才失败、handoff 状态行整组（含 `Main Agent` 标签与重试失败句）入字典；报错对话框的剪贴板与日志揭示三句入字典。

**Store 层不再写散文**：设置项写入失败原本在 store 里拼英文句子，现在以 **i18n key 跨界**（`settings-write-error-keys.ts` + `isSettingsWriteErrorKey()` 守卫），渲染层单点翻译；传输层原始报文（如 `ENOENT: …`）原样透传，有测试守住。同一形状推广到项目文件视图（`PROJECT_FILES_LOAD_FAILED` + 守卫 + `PageLoadError` 单点翻译）、校验文案（`validation-message.ts` 改为注入 `t`）、项目列表加载失败（`isProjectLoadErrorKey()`）。

**文案归位（四波，共 128 个 key × 9 语）**：设置面、workspace 面、共享组件面、图形拾取/数字化面板、组学面板的硬编码文案全部入字典。其中有**反向坏文案**：工作台的 `会话书签`/`文献库`、文献库的 `解除 PDF`/`挂载 PDF`、组学与图形面板整片中文——中文用户看不出问题，其余八种语言的用户看到的是母语界面里嵌着中文；同样地，读屏用户面对八个对话框只能听到一句英文 `Close`。权限范围确认对话框原本用英文片段拼句子（`Allow {subject} {scopePhrase}?`），语序一变就出洋相，改为 11 个 key 组合。共享层的句子（`src/shared/omics-preview.ts`、`figure-pick-session.ts`）改为**注入 `t` 并自填占位符**，图形拾取的拒绝理由改为**带 key 抛出**、渲染层只翻译认得的 key；而给 agent 读的指令文本（`describeFullRunHandoff`、`describeFullRunProposal`）**固定英文**——同一份清单在不同语言环境下必须被同样地执行。

**首帧假空**：项目列表在首帧后一个 IPC 往返才到，那一帧直接渲染空态，等于对有项目的用户说「你还没有项目」；现在区分加载中与空。作业徽标同一形状更深一层：没有徽标既表示「这个会话没有作业」，也表示「没人读过作业列表」——而全仓只有一处会读它，于是会话一旦不在那里，**有运行中的作业也永远不显示**。徽标现在自取数据，并顺手把五处英文入字典。

**i18n 质量门是真的在拦**：本版被它拦下的包括德语 `Reviewer` 照抄英文、法语 `Provenance` 与英文同形、西语 `error` 同形、中文「专家」不合术语表（应为「专才」）、中文「该文件」被判机翻腔、`GB RAM` 未本地化。新增值不得与英文同形、zh 必须真翻译、zh-Hant 不得出现简体字、占位符必须成对。

## v1.69.0 — 2026-09-23（按下的键都有去处，关掉的面板把焦点还回来）

**这一轮修的是「最后 30 厘米」：能力都在，键盘用户走不到。** 一次入口层审计清点了 431 条渲染层合同面、33 条无任何 UI 调用点的能力、61 处集合表面、11 个没有键盘出口的浮层；本版先把键盘闭环收敛掉。

- `#` 会话选择器此前是个按键黑洞：弹出之后 ↑↓ 不动、Enter 既不选中也不提交（composer 把键留给选择器，而选择器不认领）。现在 ↑↓ 选择、Enter 插入、Esc 关闭，无匹配时 Enter 让位给消息提交，高亮行带 `aria-selected`。
- 四个自建浮层（文件夹授权、文献库、会话书签、标注）改走仓内既有的 Radix 对话框壳，白得 ESC、焦点陷阱与初始焦点；会话信息卡自认领 Escape，同时把 Escape 让给上层对话框。
- **焦点归还补的是系统性缺陷**：Radix 把焦点还给 `Dialog.Trigger`，而本仓对话框几乎都由外部 state 打开、Root 里没有 Trigger，关闭后焦点掉到 `<body>`，键盘用户被扔回文档顶部。新增 `components/ui/dialog-focus-restore.ts`：打开的那一瞬记下 opener，关闭时还回去；带退场动画的对话框在焦点陷阱消失后再断言一次。
- 文件浏览器可以在键盘上走完：跳转菜单打开即取焦、Esc 归位，目录列表从「每个文件一个 Tab 停靠」改成单一停靠 + ↑↓/Home/End。
- 预览的文件动作菜单（复制路径 / 下载 / 存为产物 / 提取图表数据 / 导入文献）此前只有右键一条路：现在 `Shift+F10` 或 Menu 键同样唤起，打开即取焦、Esc 归还。
- 命令面板与消息铃铛关闭后把焦点还给打开它们的控件。
- `e2e/accessibility.spec.ts` 新增「键盘闭环」用例，在真机 Electron 上验证：打开即取焦、Escape 收起、焦点回到 opener、`Shift+F10` 开菜单并落在首项——四例全绿。

**方法学修正**：Playwright 跑的是 `out/` 里的构建产物，改完源码必须先 `npm run build:e2e`，否则测的是旧包。本轮曾据此把旧包的行为当成本轮结论，白跑三轮。

**一条待办发现**：搜索按钮上印着 `⌘K`，但真机上按 ⌘K 打不开面板（已配好 agent、进入 workspace 后按键同样无反应，按钮点击正常）。已记入入口层排期，不在本版范围。

## v1.68.2 — 2026-09-23（这道围栏在 Windows 上原本形同虚设，现在真的拦得住）

**v1.68.1 的路径围栏在 Windows 上是失效的**：`C:\Users\…` 这类盘符路径压根没被当成路径，根目录之外的读写一律不判——而 macOS/Linux 一切正常，本地门禁全绿。

- 根因：围栏脚本由模板字面量生成，源码里写的反斜杠会被**再转义一次**，落盘后 `[\\/]` 变成 `[\/]`——那行正则只匹配 `/`、不匹配 `\`，于是盘符路径从未被识别，围栏在唯一必须靠它划界的平台上等于不存在。修法：生成脚本里不写反斜杠字面量，改用 `String.fromCharCode(92)` 构造、`charCodeAt(…​) === 92` 比较。
- 同时补齐其余 Windows 平台假设：系统目录从环境变量取（`SystemRoot`/`ProgramFiles`/`ProgramFiles(x86)`/`ProgramData`，盘符随机器而定），路径比较前折叠大小写并按分隔符规范化，同一目录的两种拼写不再得到两种判定。
- 新增 Windows 语义的本地模拟测试：让 node 以 win32 上报并交出 `path.win32`，在任何机器上验证「盘符路径在根外被拒 / 根内放行 / 大小写变体放行 / 系统目录放行 / `find /` 仍被拒」。这类错误不必再靠一轮 30 分钟的 Windows CI 才能发现——本次它就是这样在几秒内被复现的。

## v1.68.1 — 2026-09-23（更新提示不再依赖尚未上线的镜像站；agent 的手脚被围栏在本机自己的目录里）

**在装的 v1.68.0 用户从此能收到更新提示——这条修复补的正是它自己看不到的那个坑。**

- 更新发现回退（`electron-updater-strategy`）：`electron-builder.yml` 的 `publish` 指向镜像站
  `statics.zerolink.com`，站点还没部署时更新检查直接失败，用户永远等不到提示。配置保留不动，
  检查失败即改用 GitHub Releases 再试一次；重试仍失败则报**第一次**的错误（它点名的才是本机实际配置
  的 feed），未打包运行不重试。
- 更新说明取真实内容（`scripts/generate-version-manifest.mjs`）：`version.json` 的 `notes` 一直写着
  `See the release notes on GitHub.`（32 字符），应用里只能看到这句废话。生成器改为从该版 CHANGELOG
  取正文，已发布的 v1.68.0 资产也就地刷新为 1,533 字符，`downloads`（url/size/sha256）逐字未动。
- 路径范围收敛（新增 PreToolUse 路径门 `src/main/settings/path-guard-hook.ts`）：真机上 agent 为了
  找一个项目文件跑 `find / -iname "…"`，扫过整台机器——包括**另一个安装**的数据目录，读到的内容还会
  当成"项目文件"汇报。现在文件工具与 shell 调用里写出的绝对路径都要过判定：落在本机数据根/配置根之内
  放行；之外拒绝；`/etc/passwd` 这类凭据读取照旧拒绝；`/usr`、`/bin`、TLS 材料等系统路径照常放行。
  拒绝信息直接告诉 agent 项目文件在哪个目录、需要别的目录请让用户挂载——它据此自行收手并回来问，
  而不是绕路。每次判定写入 `hooks/path-guard-decisions.jsonl`，可审计。
  同一目录的两种拼写（macOS `/tmp` 与 `/private/tmp`）先解符号链接再判定：这条是实机跑出来的——
  首轮验证时 agent 报告 `/private/tmp/...` 被拒而 `/tmp/...` 能写。

## v1.68.0 — 2026-09-22（文献 PDF 随包走，导入的会话真的只读；新加的提供商必须先连通才算保存）

**`.science` 会话包开始携带文献库的 PDF——并且这一半是真机验证过才敢说的**

- 包原本只带会话引用的引文，论文本身留在原地。现在文献库的 PDF 以 `references/…` 随包同行，与会话自身文件同一套规矩：注入式存储、字节上限、同名给稳定后缀、没传过去的一律具名（`reference-unreadable:` / `reference-omitted-too-large:` / `reference-pdfs-not-requested:`），绝不静默丢弃，也绝不写半截。它们记在自己的清单字段里，读包的人仍能分清"会话产物"与"会话引用的论文"。
- **真机验证抓出三处"单测全绿"的缺陷**：① 导出侧去"被替换版本的历史表"里找**当前**附件，而那张表只记录被换掉的旧文件 ⇒ 每个包实际携带 0 篇论文、精简包连计数都不报；现在只从文献记录行读取，并有读源码守卫防止回退。② 导入写出的会话文档缺容器时间戳 ⇒ 应用物化出的会话图缺少校验器必需的帧/分支时间戳 ⇒ 文档被判非法并隔离成 `.invalid-`，"导入成功"落成一个打不开的会话；现在包携带会话时间戳、更早的包按导入时刻落章，并有"写出的文档必须通过应用自身的会话文件校验"的往返测试（含反向基线证明时间戳就是承重件）。③ 只读姿态写进了 sidecar，却**没有任何读取方** ⇒ 导入的会话可以照常被 resume 执行；现在 resume 这道唯一的"可执行之门"按名拒绝，并以"本机自己的会话照常恢复"作为对照。
- **真机取证**：隔离实例 + 真项目 + 真 arXiv 论文（`1706.03762`）+ 真 agent 回合（agent 用内置 PDF 工具读满 15 页并核对摘要数字）→ 真导出 → 用 python `zipfile` 独立重算每一条的 sha256 与字节数，并与**应用自己记录的校验和**、磁盘上的源文件三方对齐（29/29，含"包内 PDF 与源论文逐字节相同"）；导入后读回 7 条消息的完整转录、保留来源机时间戳、resume 被拒；导入前后各做一次全量会话扫描，确认只读记录不再被当成会话文件隔离。

**导入的会话真只读：连"记录会被扫描隔离"这一层也修掉了**

- 会话目录扫描把 `<会话>.import.json` 当成会话文档读，归一化失败后**当作损坏文件隔离**（重命名成 `.invalid-…`）——只读记录就此消失，下一次全量扫描后导入会话恢复可执行。现在它与会话摘要一样被排除在扫描之外，并有测试断言"扫描后文件仍在、无任何告警、只列出会话本身"。

**新加入的提供商：连通才算保存，失败给出可执行的下一步**

- 设置页新增 provider 的保存门：新提供商必须连接可达才算保存完成；保存门抽成可注入的纯函数（此前测不了），并配 5 条单测。
- 修掉一个真实崩溃：settings-store 的快照映射漏了 `agentFrameworks ?? []`，缺它会让设置页在 `selectFrameworkApiEndpoints` 处整页崩。
- `providerReconnectPending` 进入 ACP 状态快照（main → 协调器 → 渲染层初值）；provider 面板显示"正在把提供商变更应用到运行时"；provider 连不上时，失败回合给出**可执行**的下一步，并与设置卡片共用同一句恢复指引（九语言）。

**质量基线**：双 typecheck、全树 lint（0 error）、全量测试、PR 门禁与 Nightly 绿。

## v1.67.1 — 2026-09-20（服务名不再需要"猜转义"，慢读断言不再假红）

**笔记本服务改名消歧：`purescience_notebook`（旧名继续有效）**

- 框架会把服务名嵌进给模型的工具名（`mcp__<server>__<tool>`），而**各家对连字符的转义不同**——有的转成单个下划线，有的转成双下划线。于是带连字符的服务名意味着"可调用名取决于谁在渲染"：模型发出的名字在运行时不存在，就得到 `No such tool available`，且会话无法把这次调用归属到任何工具。现在规范名**无连字符**，任何框架都不必猜。
- **旧名 `purescience-notebook` 进入别名表**：改名之前写下的授权、配置或框架回报的工具名，仍然解析到同一身份——不需要重新授权，笔记本授权的**语言维度与显示标签**都保持不变。
- 顺着改名排查"所有以服务名为键的地方"，测试抓出并修掉了三处退化：Codex 的叶名别名不再把 `execute` 认成 `notebook_execute`；权限中介的笔记本常量与它自己的下划线归一化比较不再匹配（导致权限键丢掉语言后缀、标签退化成原始身份串）；散文渲染器只重写了框架的转义拼法、没重写规范拼法——于是指引会告诉模型去调一个该框架**从未注册**的服务名。
- 识别端现在接受单/双下划线两种转义；散文里提到的服务名一律重写成该框架真正看到的名字。

**测试可靠性：慢读断言的信号不再与噪声同量级**

- `storage:get-info` 的分段断言一天内两次在**没碰 storage 的提交**上失败（本地 234ms vs 界 80ms、Nightly 94ms vs 83ms）。它想验证的是"仪器把慢读归到对的段"，靠的是一个故意放慢的读取；而旁边三个探针要走真实文件系统，机器一忙就能涨到几十毫秒。现在这个信号比噪声高一个量级，下界随信号走——**真正误归因仍会失败，机器忙不再假红**。

**质量基线**：双 typecheck、全树 lint（0 error）、全量测试、Nightly 与 Windows CI 双绿。

## v1.67.0 — 2026-09-20（测序数据先核验再谈分析，批量操作逐项留痕）

**ENA / FASTQ：只核元数据与可达性，不下载大文件冒充分析**

- 新增 `omics_archives` 连接器：读 ENA run 元数据，并**逐文件给出归档自己声明的字节数与 MD5**，同时探测位置是否可读。它绝不为"看起来分析过了"去下载 FASTQ——本轮核实响应形状的那次真实运行，光是一个 run 就列出 4.98 GB 与 5.21 GB 两个文件，这正是"不下载"的意义。
- 真实响应形状照实处理：`fastq_ftp` 是**不带协议的主机名**，字节与 MD5 以分号分隔对应多文件；查不到就报错，不当作"没有数据"。

**批量启停：一次改多个，但每一项单独留痕**

- 设置面板新增"全部启用 / 全部停用"，作用于**当前显示的连接器**（界面写明数量）。结果**逐项回报**：已更改、本就是这个状态、未生效（附原因），并提供汇总行；服务层按项复用单连接器那条路径，整批只触发一次连接器刷新。
- **未知 id 与空 id 具名拒绝且不写入**——拼错不会留下一条永无人读的设置；幂等项不重写，因此"我们改了"与"本来就是"分得开。
- 真机验收：真实窗口里点批量停用，从应用自身设置库读回确认每个连接器都真的关掉、且连接器 id 集合不变（面板会乐观更新，因此界面看起来关掉本身不构成证据）。

**命令行：`init` 与 `doctor`，但就绪判定仍然只有一处实现**

- `init` 只创建/确认配置根（模式 0700），不写任何设置、不覆盖任何文件，对已安装的环境是安全的；`doctor` 只报**本机实测事实**并逐行带证据：配置根能否真的写（探测文件而非假设）、状态文件里记录的 pid 是否仍存活、web token 是否存在（**只报长度，绝不打印值**）、能否定位已安装的应用二进制、以及当前 Node 是否满足**包自身 `engines` 声明**的要求（读包，不硬编码）。它仅在自身检查失败时非零退出，并明说就绪结论来自 `ready`。
- 仓库本就写着"就绪判定只能有一处实现，第二份实现正是失败模式"；因此**能力管理不进 CLI**——`connectors enable` 仍然是不存在的命令，CLI 只提供只读投影与本机预检。

**分发线：首屏给真机产物，落地页覆盖 9 种语言**

- 两份 README 首屏改成**产品真机产出的图**（EGFR T790M IC50 曲线）并指向证据目录；新增德、西、法、日、韩、俄、繁中 7 份落地页（真机截图 + 三步上手 + 真实安装包体积），深内容指回中英文全文，9 种语言导航互相可达。

**质量基线**：双 typecheck、全树 lint（0 error）、全量测试、Nightly 与 Windows CI 双绿；真机验收车道 **10 条**。

## v1.66.0 — 2026-09-20（PDF 读全，富集可复算，参考序列带校验）

**PDF：图与图注也读得出来了**

- 此前只读表格。现在图的识别按页内的图像占位做，**图注只按邻近关联、绝不编造**：文档没写标签就如实回 `captionSource: 'none'` 并计入 `withoutCaption`；关联来的图注附一句"需自行确认它标的就是这张图"。装饰性小图（规则线、logo）**计数**在 `skippedSmall` 而不是静默丢弃，每个图都说明自己的框是**轴对齐框**（旋转与斜切未建模）。
- 规则全在共享层（图注标签识别、跨文本项成行、最近图注判定、图形矩阵→矩形），主进程只负责喂数据；与 `pdf_tables` 同规格地进入 agent 工具 `pdf_figures`、宿主命令 `pdf:figures`、本地 Web 面与 preload 路径。
- 真机验收：夹具让假 agent 经应用自己的产物工具写出一页 PDF（2×2 图像以 200×150 点画在 (100,500)，下方 `Figure 1. Measured response`），spec 走应用自己的通路读回图框、图注与警告。

**基因集富集：外部服务的说法不算结论，数字要在本地算得出来**

- 新增纯函数引擎（对数域超几何上尾、Lanczos lgamma、Benjamini–Hochberg（含单调化）/Bonferroni、fold enrichment）。**背景与校正写进结果**：用默认背景时会明说"若与研究设计不符请显式给背景"；不做校正时会明说"这是原始 p 值，不能直接按 0.05 判显著"。
- g:Profiler 连接器两种模式：给 annotation 则**全程不出网**；只给基因则取回条目后**按服务自报的四个整数本地重算**，两侧 p 不一致的行进 `mismatches` 并同时给出两个数字，缺字段的行标 `recomputed:false` 而不算作一致。
- 上尾概率与 BigInt **精确有理数**独立对拍；边界（基因不在背景内、重叠过少、查询重复、背景为空）全部计数并具名。

**NCBI 参考工具：序列带摘要与校验，装配体不默认**

- 三个工具（取参考序列／取区域／列装配体）加入 `genomes` 连接器。序列返回**收到的字节的 SHA-256 + 字节数 + 碱基数**，可重新拉取比对；超过 20 万碱基的载荷**具名截断**（长度与校验值仍是整段的）；空回答是错误而不是空序列。
- 可传 `assembly` 作断言：**defline 没写它就直接失败**并说明拿到的是什么，因此这一侧不可能悄悄默认 GRCh37/GRCh38；列出装配体是为了让选择显式发生，它自己不做选择。
- 形状以**真实服务**核实（不只是 mock）：efetch 返回 `>NC_000007.14:117199500-117199540 Homo sapiens chromosome 7, GRCh38.p14 Primary Assembly`（装配体是裸写形式，正是解析器读的那种）；Datasets 报告返回 human 的 `total_count 2583`、`accession GCA_000001405.29`、`assembly_name GRCh38.p14`、`refseq_category 'reference genome'`。

**书签：三种锚点全部闭环（本轮与上一条一起发布）**

- `preview-text`：预览里**选中即存**，锚点同时带版本 id（证据链）与**规范定位符**（跳转用）；空选区不写空条目，只提示先选文字。
- `pdf-region`：PDF 工具栏出现"区域书签"开关（**仅当可溯源时出现**），页面前置薄拖拽层——规则全在共享层（归一化到页自身盒、**小于最小面积的手势按点击拒收**、无有效页面盒拒收），存下的区域带页码、几何框与定位符。
- **读侧一致**：列表对这两类都给出跳转 → 用定位符问 lineage → **打开当时那一版**；**那一版若已不存在就什么都不打开**，绝不用"同名的文件"顶替读者存下的引用。
- 真机验收：`pdf-region-bookmark.spec.ts` 用真指针事件在真 PDF 上拖框、读回应用里存的锚点，并要求跳转用的定位符能解析回它命名的版本。
- 途中挖出并修掉一个真缺陷（`0a0bd39`）：从转录卡片建立的预览会带上产物库**从未见过的** `artifactId/versionId`，用它写出的定位符解析不到任何东西（点开是空预览且无报错）。现在**写前先落实身份**：先向产物库核对，不成立则回退到会话文档里该文件的真实身份，两者都不成立就不写并明说。

**质量基线**：双 typecheck、全树 lint、全量套件、Nightly 与 Windows CI 双绿；certification 车道现有 **9 支**真机规范（含本轮新增的图注抽取、PDF 区域书签、引文样式与分叉）。

## v1.65.0 — 2026-09-20（引用都对得上，副本都追得回）

**引文样式层：真样式文档全部保真，缺什么都说出来**

- 9 个内置样式（GB/T 7714-2015 首选、逐字节兼容旧渲染器；APA 7 / Vancouver / IEEE / Nature / AMA 11 / Harvard / Chicago 18 / MLA 9），加一个**自研的受限 CSL 子集编译器**：没有许可就不安装；编译器认不出的要素**逐个具名**，绝不冒充保真。
- 拿 8 个真实 `.csl` 文档（研究者真正会导入的那种）全量导入验证：修前 **5 verified / 3 partial**（APA、MLA、Chicago 的作者整段消失），修后 **8 / 8 verified**，作者与标题同时在。根因不是"保真度不够"，而是缺 `<substitute>` 语义——这些样式的 author 宏先问 `composer`/`guest`，再回退到 `author`。
- 补 `<substitute>` 的过程暴露了一个**更糟的自伤**：内联编译的替补共享主展开的宏缓存，展开更深，于是把一支被深度截断的宏缓存起来，主路径再取就是空——APA 从"有标题没作者"翻成"**有作者没标题**"。用 `git stash` 只暂存该文件做 A/B 确认是我引入的，改成替补用独立展开上下文；回归测试**双向验证过牙齿**（修好通过 / 退回共享上下文则 `expected ''` 变红）。

**会话分叉：先量后复制，源会话逐字节不变**

- 信息卡里两步走：先量（消息数 / 智能体回复数 / 产物引用数 / 上传数，外加**具名列出不携带的三项**：活动链、运行状态、产物版本不复制只引用），确认是另一个独立动作。
- 副本每条消息换新 id，回复链重映射到副本自身，产物引用继承（证据链跟着走），活动链与运行状态清空、状态置 `idle`，并带 `forkedFrom` 溯源；**源文档逐字节不变**（有测试断言）。`.science` 导入的只读会话可分叉：副本丢掉只读姿态、保留来路。
- 真机上挖出一个真缺陷：信息卡在打开期间会被重新挂载，已量好的清单原存在组件实例状态里 → 读者的测量结果会被后台会话更新抹掉。改为按会话留存，并加单测钉住"重挂载后数字仍在"。

**书签三种锚点：两种闭环，一种立案**

- `message-text` 与 `preview-text` 现已闭环：预览里选中一段就能存（锚点带该版本的 id 与规范定位符），列表里点它**跳回当时读的那一版**；那一版若已不存在就**什么都不打开**——绝不用"同名的文件"顶替读者存下的引用。空选区不写空条目，指向空处的定位符在校验层直接拒收。
- `pdf-region`（页内拖框）**立案未做**：模型与校验在、创建路径没有，"等同不存在"，按候选交付不凑数。

**会话信息卡与钉住**：标题、描述、状态、消息与回复数、去重产物数、创建与更新时间，全部从同一份 transcript 计算，卡片不与读者看到的内容打架。

**质量基线**：双 typecheck、全树 lint、全量套件 **1049 文件 / 13996 测试通过**（191 跳过）、Nightly 与 Windows CI 双绿；新增两个真机验收规范（引文样式导入导出、会话分叉）并入 p0 车道。

## v1.64.2 — 2026-09-19（撤掉多余的那层，说清正在测量）

**存储面板：正在测量，而不是 0 B**

- 冷缓存那趟磁盘 walk 还在背后跑的时候，面板曾把占位渲染成 `0 B`——那会被读成"数据没了"。现在显示"正在测量"。真机（打包产物 1.64.1 上复验）：首读 **1 ms / `pending`**，20 s 后 **7,363,622,681 B / 5 类**（`pending` 消失），面板全程不出现 `0 B`。

**撤掉 `ProjectFilesView` 上多余的那层限流**

- 上一版给文件面板的预览批量加了一层共享限流器；这一版**撤掉**它。按调用者指纹（面板请求带 `projectId/sessionId`、转写卡片不带）做的 A/B：在产物最密集的项目（31 产物、grid、全部展开）上，**有闸与无闸峰值逐项相同**，而且面板的瓦片走的是 `preview-resources:acquire`（各 10 次、**峰值 4**）。
- 根因：这条扇出**一直有上限**——面板的预览读取器是 `createProjectFilePreviewReader(…, PREVIEW_READ_CONCURRENCY = 4)` → `createKeyedRequestReader`。多出来的第二层只带来耦合（一条悬挂读会占住槽位）。`lib/request-limiter` 保留在真正无闸的调用点。

**判据「引擎排队 `>100 ms`」正式收口：本机不可复现**

- 用真实最重负载复测：并发读语料里**最大的 12 个已固化产物**（合计 **19.9 MB**、最大 6.3 MB、每个解析都含一次查询 + 整文件读取 + checksum 校验）——12 次全部成功、wall **103 ms**（各自 ~102 ms ⇒ 引擎**并发**服务，未见串行排队）；同窗引擎金丝雀 **7 ms**；main 自身的慢 resolve 仪器（50 ms 阈值）**一条未触发**。
- 结论：U11 的 135/124 ms 判为**环境特异**，只保留方向与量级（扇出 52→1、启动期金丝雀 42→3 ms、无界 vs 有界排队深度 ~6×）。
- **同时记下一条未解释的真实现象**：渲染层实测单次 resolve **~102 ms**，而 main 三段之和 <50 ms（仪器未触发）⇒ 差额落在 **IPC/调度**，不是那三段；给 resolve 加 IPC 侧分段计时是下一个入口（已落档）。

**打包版复验（生产构建）**

- `dist/mac-arm64/PureScience.app`（1.64.1 / 源码 `433a015`）上复验：存储首读 **1 ms（pending）→ 20 s 7.36 GB / 5 类**；Home chip **177 元素 / 16 种**（与 dev 逐项一致）；四个慢读仪器（`listFiles segments` / `kinds read` / `project files read` / `artifact version resolve`）**全 0**。

## v1.64.1 — 2026-09-19（首屏读一次，探测问一次）

**Home 的 chip 不再每项目一问**

- 启动期 Home 对每个活跃项目各发一次 `project-files:list-files`：真机实测（52 个项目）启动窗内 **52 次读**，每一次都排在同一条引擎队列里。现在由**一次** `project-files:list-kinds` 覆盖全部项目（窗口函数取每项目最新 30 条、按共享规则推导 kind）。
- **真机数字**（同一语料、同一驱动、同一仪器，前后各跑一遍）：启动窗内 `list-files` **52 → 0**、`list-kinds` **0 → 1**；每次 Home 挂载的扇出 **52 → 1**。chip 渲染逐项一致：**177 个元素 / 16 种**（`MD/JSON/PY/PNG/CSV/PDF/SDF/GZ/XLSX/NPZ/HTML…`），在真实语料完整副本上复验同值。
- **排队指标**：与查询同窗启动的平凡 `SELECT 1`（引擎金丝雀）**中位 42 ms → 3 ms**；chip 相关慢读条目 **1/3 → 0/0**（文件面板自身的读保留）。

**产物卡片的"还在不在"从 40 次读变成 2 次批量**

- 会话转写里**每条产物卡片各发一次**可用性探测，而且卡片的 path 是版本定位符，所以每次探测在 main 里都走**完整内容解析**（一次查询 + 整文件读取 + checksum 校验）只为回答一个是/否 —— 真机实测 **40 次 / 峰值 15 并发 / 单次中位 22 ms**。
- 新增 `artifacts:probe-availability`：整批**一次查询**定位各版本路径、**只做 stat、不读内容**；渲染层把同一 tick 的请求**合并成一次调用**（`preview-availability`），并带结果缓存。**卡片之间互不阻塞**：一次批量失败不缓存、下次可重试，没有桥也不会悬挂。
- **真机数字**（同一驱动）：`read-preview` 的 1 字节探测 **40 次 → 0**；批量通道 **2 次**（15 条 + 5 条）/ **峰值 1 并发** / **中位 2 ms**；两次的调用数一次不少（没有丢读）。队列里不再塞满探测之后，**卡片预览自身中位 11 ms → 4 ms**。

**存储面板不再等 13.7 秒**

- 真机复现：`storage:get-info` 首读 **13,863 ms**；段级仪器指名 **`usageMs` 13,669**（占 99%，根目录解析 0 ms、可用空间 1–172 ms、设置 0–22 ms）—— 冷缓存把整趟数据根磁盘 walk 等完，而启动时的预热把同一趟 walk 起在同一窗口。
- 冷读改为**立即返回带 `pending` 的占位**、walk 继续在背后跑；面板据此显示"正在测量"，**不再把占位渲染成 `0 B`**（那会被读成"数据没了"）。实测：首读 **2 ms（pending）**、20 s 后 **7,363,622,681 B / 5 类（pending 消失）**，那条 13.7 s 慢报告消失。

**这一版同时修掉的真缺陷与澄清**

- 共享请求限流器在任务**同步抛错或返回非 promise** 时**永不归还槽位**（四次之后饿死后续所有读）—— 已修并补用例；这也是"每卡片一个读"不能被全局闸约束的原因（一条悬挂读会冻结整列）。
- `StorageUsage` 在 main 与 shared **各有一份定义**，新增字段只在一侧生效；现在合并为一份。
- 判据澄清（**不是**"已优化"）：引擎排队 `>100 ms` 高位在**真实规模完整副本**上最深只有 4 ms，**本机不可复现** —— 只保留方向与量级（扇出 52→1、金丝雀 42→3 ms），检测手段（`PURESCIENCE_DB_CANARY=1`）随本版入库。

## v1.64.0 — 2026-09-18（列表读只碰索引，不再解析文档）

**两级目录的另一半：负载已经剥掉，这一版把延迟也剥掉**

- 上一版把列表的**载荷**从 55.4 MB 剥到 0.50 MB，但服务端仍是全量扫描——**真机实测** `sessions:load-all` **55,371,532 B / 1541 ms**（59 会话），每读一次列表就要重新解析全部文档。
- 列表层现在读**盘上索引**（`<session>.json.summary.json`，随会话保存写入），以文档自己的 **size + mtime** 作失效判据：指纹相符 ⇒ **不打开文档**；缺失/过期/旧格式 ⇒ 只解析**那一个**并回写索引。
- **真机数字**（真实语料副本 + 真机实例 + web RPC）：`sessions:list-catalog` 返回 **569,858 B（0.54 MB）**，冷启动第一次 **68.2 ms**，稳态 20 次 **p95 23.2 ms**（中位 12.0）；服务端计数 **21/21 次 `indexHits 59 / parsedDocuments 0`** —— 文档一次都没被打开。
- **外部改删必被发现**：目录遍历仍是文件清单的唯一来源。实测外部改一个文档 ⇒ 只重解析那一条并立刻反映到列表；外部删一个 ⇒ 会话数 59 → 58、条目消失、零解析。
- **索引缺失回退全量**：删掉全部 59 个索引文件后，第一次调用 **503.5 ms**（`hits 0 / parsed 59`，重建），下一次 **38.0 ms**（`hits 59 / parsed 0`）。索引格式升级（v1 → v2）同样走这一趟，之后恢复零解析。
- **索引存的是列表投影本身**，不是元数据切片：切片里没有 messages，`messageCount` 与最后一条 agent 回复**推不出来**——靠切片重建会把每个会话都显示成 0 条消息。
- **协调不再搭列表读的车**：派生状态协调（上传升级 / 产物恢复 / 文件索引 / 权限清理）需要**每个文档的全文**，塞不进一次元数据读。它改为**进程启动跑一次**（真机日志：`startupCleanupEligible: true`、`status: ready`、59 会话、0 降级），仍在启动边界上，只是不再挂在用户读列表的那一跳。

**真机验证顺手抓到一个会丢数据的缺陷（护栏）**

- 按既定判据在真实窗口里走用户路径（首页 → 工作区 → 会话菜单 → Pin）：**对一个从未打开过的会话执行 Pin，把它的会话文档截断了**——9 条消息 → 0（扁平列表与会话图都是 0），只剩元数据。
- **根因**：护栏的「仅摘要」标记放在按**对象身份**索引的 `WeakSet` 里，而 store 的每次更新都会换新对象（`togglePinned` 就是 `{ ...session, pinned: !session.pinned }`）⇒ **第一次不可变编辑就把标记丢了**，saver 于是把没有消息的摘要写进了文档。它不是「不可绕过」，而是**任何编辑都能绕过**。
- **修法**：标记改为**按 session id**，并在三条「会话重新可写」的路径上显式解除（读到文档 / main 推来带内容的投影 / store 重置）。
- **修复后同一真机路径复验**：Pin 未打开的会话 ⇒ 文件**逐字节未变**（323,416 B、sha256 相同）⇒ **保存被拒**；打开该会话后再 Pin ⇒ 写入生效（`pinned: true`），且 **9 条消息仍在**（列表与图都是 9），索引同步一致。
- 两条回归用例钉住机理（**编辑换对象后仍拒写**、**读到文档后同一编辑放行**）——它们在旧实现上必红。

**证据**：`docs/evidence/2026-09-18-incremental-catalog-index.md`、`docs/evidence/2026-09-18-lazy-guard-live-verification.md`，含可复跑的测量脚本与驱动探针。**诚实边界**：数字来自 dev 构建（可作上界）；语料是副本，真实根未被指向；启动协调那 2.6 s 仍是全量，「只处理水位之后变化的会话」是独立单元。

## v1.63.0 — 2026-09-18（让每个结论都经得起追问）

**环境锁：从"永远比不出来"到真机 `applied`**

- 复现比对里那句 `env lock` 一直报 `not-applied`，**不是因为环境不同**，而是因为比的是 `sha256(整个环境清单文档)`——而文档里带采集时间戳。**真机证据**：同一会话、同一解释器、相隔几分钟的两次运行给出 `b6d0cf04…` 与 `cc324682…`。**一个永远不可能成立的比较不是检查。**
- 现在两个值各司其职：**存储/校验**用文档哈希（读方按它取文件并重哈希证明未改动，语义未动），**比较**用**环境指纹**（运行时名/来源/版本 + kernel 种类 + 平台 + 架构 + **排序后的包列表**）。指纹贯通 capture → 运行记录 → 版本证据（`producer.environment_fingerprint`）→ 重跑判定。
- **真机结果**：`environmentLock: applied`（此前不可能出现）；两侧**任一没有**指纹时仍报 `not-applied`——含义是**"无法比较"**，从不冒充"环境一致"。
- **重跑起在记录所用的运行时上**：重跑会话在**首个 cell 之前**，从**记录会话自己的 run.json** 读回当初绑定的 runtimeId 并绑上（不是从重跑会话读）；记录已不在 ⇒ 照常跑、保持未锁。实测同一版本的绑定前 1567 ms → 绑定后 540 ms，`reproduced` 保持。
- 途中我自己捅过一个回归并当场抓住：把规范化摘要直接当发布摘要，会撞坏读方不变量，**每次采集都塌成 `environment-manifest-publication-failed`**、证据丢整个环境块。**1573 个单测全绿**，是真机复验抓到的。

**未发布的产物：每一条都说清自己为什么没发布**

- 启动协调按名字标注永久无法落定的版本：`no-publication-intent`（从未写下发布意图）/ `ambiguous-turn-message`（意图在，但那个回合无法归属单独一条回复，**按设计拒绝猜**）⇒ 标注为未发布，**保留行与字节**，绝不悄悄删、绝不标成已发布。
- **文件面板逐条显示原因**（9 语言），没有原因的行只显示分区总说明——**"原因缺失"不渲染成两种原因之一**。
- **回填**：列存在之前已具名的版本按同一判据补齐（**真机：26/26 全部 `ambiguous-turn-message`**，与"26/26 都有发布意图 marker"的实测一致）。
- 真机核验同时抓到：启动协调里 **7 处静默 `continue`** 全部具名并计入日志；以及迁移**只在约束名缺失时重建表**，导致标注撞旧 CHECK、**整趟启动协调失败**——已修（值集变旧即判陈旧）。

**PDF 表格抽取：接上真入口，并在真机上把列模型修对**

- 抽取器**早已写好却没有任何生产调用者**（按本仓规矩不算完成）。现在接成真链路：解析层保留带坐标条目 → `PdfService.tables()`（几何优先、退弱法时**具名**）→ **agent 工具 `pdf_tables`**（MCP → RPC → 应用命令 → preload 与契约目录全套 pin）。
- 真机过程中抓到两个缺陷：① 解析器**采集了条目却没交出去**，服务退到弱方法 ⇒ 任何 PDF 都是 0 候选（in-process 复现：direct 1 / service 0 → 修后 1/1）；② 列聚类被**居中排版**切碎，真表格出 10 列。护栏最终改成**拆格阈值的反面**（拆格用 `>12` 判新格，合并就用 `≤12` 判同列）+ 共现判据（任一行同时填满两列 ⇒ 不是同一列）。
- **真机达标**：同一份真表格 PDF 经 agent 调用返回 **`7 rows × 4 columns`**，表头 `| Gene | log2FC | padj | Cluster |`。
- **保留的边界**：候选不是转写，工具每次都带 `candidate-extraction` / `verify-against-source`；达标只覆盖**矢量文本表格**，文本表格/图片表格仍是 0 候选（方法边界）。

**流畅度：先把"卡"变成数字（优化在下一版）**

- 基线（真实实例 + 真实数据根，59 会话 / 60 MB）：`sessions:load-all` **中位 1.37 s、返回 55 MB**，而**同样的 63 MB 从磁盘读只要 10 ms** ⇒ 瓶颈在**解析/归一化/序列化**，不在 I/O；启动协调 1.27–1.60 s（启动那趟曾见 6.75 s）；`search:query` 首次 1.17 s、预热后 ~65 ms；产物列表 40–70 ms。
- **明写未量到**：文件面板通道（我给的参数形状不对）、会话级产物（Electron 专有面）、**渲染层**（headless 实例量不到）、冷启动（dev 构建含重建，不能当打包版）。
- 优化（列表不搬全量消息体、目录缓存 + 事件失效、启动增量、渲染层 CDP 采样）**带验收口径立案到 v1.64**。

**质量基线**：双 typecheck、全树 lint、全量测试、Nightly + Windows CI。

## v1.62.0 — 2026-09-17（说不清的结论，这一版都说清了）

**没落定的产物版本，现在有名字（2.1）**

- **落定竞态**：运行时段"还没落盘"与消息"还没落盘"原本处置**相反**（一个当终局无效证明、一个当可重试）——同一种现象，现在同为**有界重试**（3 次，每次先重写会话）；**段存在但属另一智能体帧**仍是终局；**证明没有被放宽**（段必须真的在已落盘图里）
- **启动协调给永久无法落定的版本按名字标注**：`no-publication-intent`（从未写下发布意图）与 `ambiguous-turn-message`（意图在，但那个回合有多条候选消息，按设计**拒绝猜**）⇒ 标注为**未发布**：**保留行与字节**，绝不悄悄删、绝不标成已发布；活进程只报告不标注（在跑的回合永不被误标）
- 文件面板新增**「已生成但未发布」**分区 + **9 语言**文案，如实写清两种原因
- 协调器里 **7 处静默 `continue`** 全部带上具名原因并计入完成日志——此前"拒绝"与"无事可做"在日志里长得一模一样

**真机取证（不是夹具）**

- 真实数据根：**26 条滞留版本全部被具名**（`finalized 445 / unpublished 26`，pending 归零，一条没删）；抽 3 条跨 2 个项目 3 个 run，在存储根上逐个量到**字节仍在**（12639 / 18016 / 10419 B）
- 现场还抓到**它自己会犯的错**：标注那一步撞上旧 CHECK 约束，**整趟启动协调失败**——根因是迁移只在"约束名缺失"时重建表，**值集变旧不会被发现**。已修（`IN (...)` 类约束缺任一已声明值即判陈旧），并按现场同一形状补了会咬人的测试

**重跑：从"说不清"到给出真结论（2.1）**

- 现场实测：重跑**没有**在隔离目录里跑代码，而评分只看隔离目录 ⇒ 结论写成"重跑完成但没有产出文件"——把**路径不一致**说成了**没产出**。现在按实测目录如实说明；适配器报不出目录时保留旧句（**不臆断**）
- 真正的修法：重跑改用**独立重跑会话**（`replay-<16hex>`，建在**记录所属项目**下），**评分目录 = 执行目录**，跑完关掉 kernel 并清理该会话目录；顺带修掉"项目名从未传给 notebook ⇒ 会话一直被丢进 `default-project`、同一会话的多次重跑共用同一目录互相覆盖"
- 同一版本：改动前 `unverifiable`，改动后 **`reproduced`（字节级 match）**；原会话文件**未被触碰**（已复核 mtime 与字节）
- **仍未应用的（明写）**：环境锁 `not-applied` —— 这次复现是在**默认运行时**下取得的，**不等于"环境已复现"**；把记录里的环境绑定移植过来是下一版的事

**质量基线**：双 typecheck、全树 lint、全量测试、Nightly + Windows CI。

## v1.61.0 — 2026-09-16（每一句结论都说得清它是怎么来的）

**从 PDF 里把文献读进来（3.4）**

- PDF 预览菜单新增「从本 PDF 导入参考文献」：抽取它引用的 DOI，走**既有**的解析与写库路径，库里出现真记录
- 单文档上限 5 条，超出**如实报数**；解析不到的**按名报失败**，一条坏标识符不中断其余，**绝不编造条目**
- 真机取证当场抓到两个手工夹具漏掉的缺陷：PDF 文本层丢换行 ⇒ 标识符吞掉下一句首词（`…nature12373.Jones`）与下一词首字母（`…abc1234A`）。修好后复跑：真 PDF → 3 个 DOI → 1 条真记录（`Nanometre-scale thermometry in a living cell | 10.1038/nature12373 | 2013 | Nature`），另两条不存在的 DOI 按名失败

**会话包先核对自己的记录（2.1）**

- 导出一直给每个成员写摘要，而**导入端从未读过**：包在途中被改、被截，清单照旧声称原样 ⇒ 现在导入前逐成员比对
- 结论按文件粒度：`differs`（有字节时给出**首个不同字节**）/ `missing` / `extra` / `not-comparable`（具名：无摘要、超上限、版本不符）；**"没检查"绝不当成"过关"**
- 重跑复现与记录完整用**两套词**（`reproduced/differs` 与 `intact/changed`），**模型重构的步骤永不认证为复现**
- 真机双腿：干净包 `intact`（4 个成员全 match）；翻转 1 个字节 ⇒ `changed` 并指名 `conversation.json` 与记录/实际摘要对照
- 真机还抓到：清单自己被当成"未声明成员"⇒ 每个正常包都误报 ⇒ 已修并加测试

**可重放产物验证与 CLI（2.1）**

- 新增重跑引擎：密封配方 → 隔离目录还原记录输入 → 经**应用自己的 notebook 执行**跑录制代码 → 逐文件比对；失败/超时/无产出/缺运行时一律 `unverifiable` 并具名原因；**环境锁只在两侧清单摘要相同才报 `applied`**（同代码不等于同环境，不许混说）
- `purescience replay <versionId>`：**CLI 入口已真机验证**（真入口 → 真应用 → 具名结论 `stopped: version-unreadable`，exit 0；缺参数 exit 2；`--json` 同一份报告）
- **未取证（明写）**：重跑本体尚未拿到端到端证据 —— 任何可及环境都不存在"带录制代码的产物"（产物版本只由 agent 回合创建；对用户实例的只读核查为 0 个产物）。一条命令的距离，条件具备即可补

**搜索：能翻页、认得出中文改写、筛选先于分页（2.2）**

- **游标分页**：每域各自偏移；游标读不出来 ⇒ 回到第一页并写 `cursor-invalid`，绝不把第一页当第二页
- **中文滑词召回**：整串不中时，要求该词的**每个二字切片都出现**才算命中，并标 `kind=segmented` + 提示「按词的部分匹配」——"原句在里头"与"碎片在里头"永不混淆
- **筛选补齐**（发送者 / 文件格式 / 文献类型）**先于计数与分页**；只对某一域成立的筛选连其它域一起排除（要某人的消息，不是要文件列表）
- 真机证据**当场抓到一个排序缺陷**：分词命中因"每个切片都算一次"反超整串命中 ⇒ 改为每个词项按最好的一种命中计一次
- 界面：筛选栏（三枚选择 + 一键清除）+「显示更多」行；顺带修掉两处**硬编码英文**（`+N more matches — show more` 原样印在所有语言里）
- 验证分工如实标注：引擎由**真机**取证；筛选交互由**对话框层交互测试**取证；面板打开的 CDP 路径**未取证**（打开事件在首页被另一对话框消费、⌘K 未到达处理器）

**WSL2 Bash 预览（2.7）：审计归档，零代码**

- 复核：真 WSL 引用 **0 处**（不敏感 grep 的 17 处是 `PreviewSlice` 一类误报）；Windows 侧 26 处 `win32` 分支 + PowerShell 解析 ⇒ 该特性要求的「PowerShell 仍为默认」由构造满足
- 收口理由与**重开条件**（先出资产签名与清理归属设计，再按"显式激活 / 资产版本匹配+就绪检查 / 命令级沙箱 / 归属追踪清理"四件套整件实现）已写入台账

**质量基线**

- 双 typecheck + lint（全树无缓存）+ 全量测试；补上被漏掉的契约计数文件（`src/renderer/web/*` 另有自己的可调用面计数，加入口时容易漏）—— 该教训已入技能

## v1.60.0 — 2026-09-16（结果自己回家 · 边界、出处与语言都说得清）

**后台结果投递下沉主进程（P2-d）**

- 完成的作业结果由**主进程**写回它所属的会话：关掉窗口、甚至作业在应用关闭期间跑完、下次启动补投，都不会再把结果困在没人看的收件箱里
- 同一批结果**一批只起一个回合**（同会话多条结果合并），会话正忙时有界等待，投递后标记已消费；**渲染端旧投递路径已删除**，并留了一条"两条路径只能有一条生效"的守卫测试

**投递台账 + 读者语言（P2-d-2）**

- 作业详情新增**投递台账**：状态、内容指纹、触发者、产物文件、更新时间，并可一键复制整行（任何人可复核）
- 控制台界面语言**上报主进程**（新设置项 `uiLanguage`，带语言标记校验），续跑消息与台账状态文案随软件语言；窗口不在时也能按上次语言写
- 新增 9 语言标签表（6 状态 × 6 原因），主进程与渲染端**共用同一份**，避免两边说法漂移

**守门技能不可关闭（P2-6）**

- 应用运行必需的技能在任何派生禁用集合里都恒为启用；**只在"关闭"方向拒绝**（`skill-always-on`），界面开关显示为禁用并说明原因，批量禁用跳过它们

**隐私三道门（P1-4）**

- **明文端点门**：非 loopback 的 `http://` 端点必须由该提供商显式开启才可用，**保存、探针、spawn 三处拦截**，最后一道在环境变量组装处（即使 `settings.json` 被手改也拦得住）
- **图片元数据剥离**：附件与**历史图片**（此前后者遗漏，属真缺陷）在发送前剥离 EXIF/GPS/XMP/IPTC/COM，只删整段、绝不重编码；真机逐字节验证：发出的字节 == 原图去掉元数据段，像素数据未动
- **远程图片点击前不加载**：助手回复里的 http(s) 图片先显示主机与"加载远程图片"按钮，点击后才发起请求

**PDF 图文表抽取（P3-7）**

- 两种方法如实分开（有坐标走几何聚行、只有扁平文本走空白分栏），结果恒为**候选**；MD/TSV/HTML 导出逐份带页码、方法、行列数与置信度，并强制标注"候选抽取/需与原图核对"

**会话包流式解压**

- 逐成员展开，峰值内存 ≈ 归档 + 单个成员（实测整包解压 +64MB → 流式 +0MB）

**命令行状态面（P3-8）**

- 新增 `purescience ready` / `runtime list` / `connectors list`（`--json`）：**复用应用自己的判定**（就绪检查、运行时清单、连接器清单走既有命令），不在命令行另造一套；已实机对活体实例取证

**目录与小事（P3-9）**

- **Codex 双版本并排**：ACP adapter 与原生 CLI 版本一起显示（此前只看得到 adapter，原生落后无从发现）
- **长会话有界**：会话写入汇聚点对保留历史设上限（只丢最旧、只在超过 5000 条时丢），丢弃数量与"记录从何时续起"写入文档并在转录顶部**明确告知读者**；未超限的会话完全不走该路径
- **技能市场批量安装**：可勾选多项一次安装——先全部取包校验，只装就绪项；需处理的包按名跳过，一条失败不中断其余，汇总列出成功/跳过/失败
- **同凭据一次性结清**：同一 MCP 工具、同一命令组的多个待决请求可"全部允许/全部拒绝"一次结清；只提供每条请求都支持的选项

**修掉一个会让应用启动即失败的回归**

- 新增应用命令未同步命令面认证计数，应用会在组合阶段**拒绝启动**（`Application command inventory mismatch`）；已在实机取证中发现并修复，认证与测试同步

**质量基线**：双配置 typecheck 干净、lint 干净、全量 14,000+ 用例通过、Nightly 与 Windows Full Test 双绿（发布时核对）。

**未入本版（如实记录）**：PDF 正文抽 DOI 移入 v1.61.0；WSL2 Bash 预览按"审计归档零代码"收口（目标桌面以 macOS 为主，且须先有资产签名与清理归属设计）。

## v1.59.0 — 2026-09-13（技能复用可查 · 监督通道真正接通）

**技能复用：从"用了哪个技能"不可知，到面板可查**

- 提供方原生 `Skill` 活动**不带名字**（实测 81 条：无 title、无 rawInput、无 locations），因此由**应用自有的 PostToolUse 钩子**采集 `Skill` 工具的输入，
  按**应用会话 id**归属（采集里的 sessionId 与持久化会话 id 一致），写入应用自有配置目录
- **采集永不阻断工具调用**：钩子非零退出会阻断调用，故脚本吞掉一切异常并恒 `exit 0`、输出 `{}`，命令带平台化成功守卫
  （`|| true` / `|| exit /b 0`）；测试用**真实 node 执行**生成的脚本，验证垃圾输入仍 `exit 0`
- 读取**有界**：4MB 上限（超限保留尾部并丢弃首行残句），撕裂行/空行/`null`/裸标量一律跳过、不抛错
- **只认名字明确者**：`toolName === 'Skill'` 且技能名非空，其余丢弃——错名会变成排行里的**假复用**；**不编造轮次**（钩子看不到轮次）
- 面板显示每技能**复用次数与失败数**，并区分两种诚实状态：无记录 →「尚无技能复用记录」；有排行 →说明**本版之前的会话未记录技能名**
- **连接器技能也在列**：agent 能加载**没有面板行**的技能（连接器物化给 agent 用），其复用此前**会被记录却无处显示**——
  新增「未挂靠」区块，排行不再吞掉真数据；9 语种译文齐备

**监督通道：三处"看起来已交付、实际没通"的缺陷**

- ① **台账在 main 边界被丢弃**：`triggerReview` 用显式解构组 options，漏了 `supervisor` —— 渲染端算出的唤醒线索**离开渲染端一次调用后即丢**
- ② **手动「请求评审」盲审、跳过即丢**：台账原先只挂在自动路径；现并入**两路共用**的请求装配，且运行时信号改为审计**真正启动后**才消费（跳过不再丢证据）
- ③ **首审不留痕**：留痕只写在修正轮的**重审路径**，首审（最常见）**没有任何记录** —— 功能在工作，却不可审计
- **活体证据**（不经 UI，跑在运行中的实例上）：`POST /rpc/reviewer:run` 携带 plan → 日志首次出现
  `reviewer: supervisor ledger attached { sessionId, wakes, handles }` → `wakes: ['repeated-tool-failure@turn:3']`、带 `activity:` 句柄

**修复**

- 采集钩子登记为 **Windows 敏感源**（仓库守卫抓到的漏登记，Nightly 与 Windows 分片同时红）
- 新增应用命令必须同时落在**命令映射 + 命令组 + handler/IPC** 三处，否则运行时清单认证在**启动阶段**拒绝
  → **应用完全打不开**（实测；单测全绿也会发生，因为证书只在启动时评估）
- 渲染端改动必须 `npm run build:web`，否则浏览器 UI 报 `Incompatible PureScience Web RPC protocol`

## v1.58.0 — 2026-09-13（自我进化闭环：记忆邀约 · 技能可信 · 监督唤醒 · 逐 server 成本可视）

本版把"自我进化"从论文概念落成可验证的机制：**跑过的经验不会自动变成习惯**，**失败经验必须带证据**，
**监督线索只在有据可查时唤醒**，而"上下文里谁最贵"第一次可以逐 server 读到。

**记忆：邀约模式（不默认开启）**

- 记忆关闭时，保存请求**不再静默丢弃**：工具返回结构化结果（`suggest: enable-memory` + 可执行指引），
  系统提示写明"关闭时不要保存，改为告知用户"；实测 agent 原话说明"未保存 + Settings → Memory 可开启"，且写到别处被拒
- 不默认开启的理由写死在设计里：记忆是模型自撰的用户画像，写错会跨会话放大

**技能可信链（学习型技能不自动生效）**

- 新增**来源契约**：`kind`（procedure / failure-mode）、`verification`（unverified / verified / rejected）、来源轮次与证据；
  **缺字段或未知值一律 fail-closed**
- **硬门控**：`unverified` 的学习型技能不参与自动物化，需显式放行（`trustedSkillIds`）——"跑一半的经验不该自动变成习惯"
- **失败模式技能没证据就拒收**；创建时就声明信任态，并由规则 `learnt-skill-trust-stated` 机械核验来自
- 修复**门控空转**：个人技能（学习型技能唯一落点）此前根本没解析信任态，等于门控只挂在半条路上
- 技能面板显示 `Unverified / Verified / Rejected` + 失败模式 + 悬停可见复现证据

**监督者唤醒台账**

- 四类信号、**每类只唤醒一次**、超预算则**降级并在报告里说明降级**；线索带句柄（`activity:<id>`），
  并明确"这些是线索不是结论，请按句柄核对"
- 台账随审计请求送达审计员（`<supervisor_signals>` 注入），**修正轮继承同一批线索**，并在主日志留痕以便事后审计
- 修掉"心跳快照被读成连续失败"的假阳性；**只有显式 `failed` 才算失败**

**输入预检：缺什么，第一轮就说**

- 计划前先核验任务引用的输入路径是否存在；缺失时**在模型读到的内容里点名**（不改用户消息、不另起回合）
- 规则 `missing-input-flagged-early`：**末尾才说 = 不通过**

**抓取授权：按域记住，别反复问**

- `WebFetch` 的域级授权注册为**持久能力**（`fetch-host:<host>`，`www.` 归一），同一域不再逐次打扰；`WebSearch` 保持一次性
- **域只从结构化 `rawInput` 推导，绝不从 title 兜底**——title 可被模型控制，否则模型将同时控制"被授予的键"与"匹配的键"

**上下文用量：逐 server 可读**

- `contextUsage.breakdown` 新增可选 `sections`（`sectionId / category / tokens`），把应用自有静态定义按 server 拆开；
  上限 64、零值不计、**明细损坏只丢明细不丢已对账的 breakdown**
- 实测：`mcp` 聚合 3865 = 六个 server 明细之和（精确吻合）；据此判定**应用自有 MCP 定义不是成本大头**（同会话 `other` 30,945、`system` 4,583）

**修复**

- 所选运行时现在真正生效；notebook 执行路径尊重持久化的运行时选择；应用内 RPC 不再被 undici 劫持
- 自动审计改为 opt-out（此前默认开启却因门控不生效而根本没跑）
- 图表规则 6/7 的证据（轴刻度、渲染图、源脚本、字号）暴露到 agent 路径，规则不再空转
- 全权限专家不再把**所有**技能钉成"被引用"，个人技能可以删除了
- 计划刷新失败不再逃逸成未处理的 rejection

**已知未完成（如实标注，不当作已完成）**

- 技能排行的**数据源仍缺**：claude 路径原生 `Skill` 活动不带名字；已定位受支持的钩子透传接缝并记录风险，尚未落地
- 监督者有两类信号**未接线**（零样本，按纪律延后，不造无法验证的解析器）
- B3/B4/B5 的实机验收止于引擎层 + 真实数据回放；"真实 agent 回合"两通道已勘明并留档（UI 配方 / RPC + 日志取证）

## v1.57.0 — 2026-09-12（引擎策略落地：取数、路由、作业、权重与回放）

本版把"引擎目录"从说明书变成可执行链路：每一次要数的动作，要么有据可查，要么明说没有。

**结构取数（AlphaFold DB 引擎层）**

- 取到即**声明为数据库预测**：引擎、条目/提供方/工具、检索时间、输入 UniProt 一并进标注，并强制写出"非实验结构，不得与 PDB 实验条目并列不区分"
- 置信度指标如实：平均 pLDDT 标注为置信度（非精度）
- **数据库无条目 → 明说无条目并指向折叠引擎**（含审批门），绝不产出猜测结构；传输失败如实报错并提示重试/远程折叠，明写不得退回定性描述

**远程作业模板**

- 由引擎目录生成 slurm 或 direct-ssh 脚本：带 provenance 头、结果必须回报的标注清单，以及「**必须人工审阅批准后方可提交**」
- 告警是推导出来的：商用受限许可、预测值、GPU 引擎遇上未报加速器的主机、按需权重、MSA 需求；未知引擎直接拒绝并列出真实清单

**ΔΔG（Phase 1，ESM 零样本）**

- **没有校准因子与来源就不给能量**：只报无量纲对数比并说明原因；符号约定写死（ΔΔG = ΔG(突变) − ΔG(野生型)，负值去稳定）
- 模型分歧在两套单位下同时报告；单模型或分歧过大 → 低置信并说明理由；未知模型/非有限值 → 「未计算 ΔΔG」
- 下载同意文案含体积、MIT 许可、缓存位置与 SHA256 校验；拒绝即未计算

**权重治理（按需下载）**

- **没有同意不下载；没有已发布 SHA256 不下载**（规格缺失或摘要不合法一律视为未解析）
- 下载后必须校验：不匹配即丢弃内容、绝不加载；**缓存文件也要重新哈希**，损坏缓存按不存在处理并清除；失败不污染缓存

**组学大文件入口**

- 预览菜单按 .h5ad / .vcf / .vcf.gz / .vcf.bgz 出现「组学大文件预览（先探后算）」；载入器**不会凭空造数**，只接受经 schema 校验的清单，非法输入给可读错误

**基准回放（SciBench v1）**

- 黄金回放基线钉住负向保证：空会话对全部用例判失败、只说不做不达标、判定确定性、记分卡给规则明细而非只给数字

质量基线：双配置 tsc 0、lint 0、Nightly + Windows CI 绿。

## v1.56.0 — 2026-09-12（科学交付链 + 引擎策略：从图取数 · 大文件先探后算 · 引擎目录与路由）

本版把"结果从哪来、能不能算、怎么交付"这条链补完整。其中标为 v1.55 的部分与 v1.56 一起合并发布（v1.55.0 未单独发版）：

**图 → 数据（Figure-to-Data，估计值必须标注）**

- 标定内核：两锚点线代/对数映射（支持外推），**退化标定直接拒绝**；分辨率由锚点间距推出并如实报告
- 拾取状态机：`x 锚点×2 → y 锚点×2 → 拾取`，顺序强制、重复/非法坐标拒绝、未标定不出数、支持撤销
- 拾取面板与入口：预览内容菜单新增「从此图提取数据（estimated）」（仅 png/jpg/webp/tiff/pdf）；面板强制确认**页码**（PDF）与图号——页码非法即整份结果不可用
- 产出契约：一律 `estimated` + **路由到审查**，CSV 首部即 provenance（来源/页码/图号/方法/分辨率/状态）并内嵌**审计裁决**（来源不完整 → 整文件标"不可用"）；`auditDigitizationForUse` 列出阻止其进入正文的具体问题

**大文件先探后算（h5ad / VCF）**

- 预览契约 `OmicsPreviewManifest`：格式识别、全量计数、显式 `subset{请求/实际/抽样}`、`fullRunRequired`；「基于 N/M 降采样」标签**由契约派生**（G6 机器化）
- 技能内核 `omics-data-preview`：h5ad 只读结构（anndata→h5py→**如实报告不可读，绝不猜细胞数**）；VCF/VCF.gz 仅标准库流式计数并把截断标为**下界**
- 交接与提案：`canAnswerFromPreview`（子集一律 false）、`describeFullRunHandoff` 固定三步、`proposeOmicsFullRun`（**requiresApproval 恒真**、已全量即拒绝提案、缺主机/引擎如实列缺、声明结果必带标注）
- 预览面板：子集预览醒目警示"不得作为最终结论"，全量入口需先选主机，无主机时给"未计算"指引而非数字

**引擎策略（拍板落地：目录 + 分层降级 + 预测标注）**

- 引擎目录 `engine-catalog`：7 引擎声明（AF DB / PDB / ESMFold / ColabFold / ΔΔG CPU 预测器 / OpenMM FEP / Rosetta ddg），每项含**输出性质（measured / lookup / predicted）**、GPU 需求、权重体积、是否按需下载、许可（含商用限制）、部署位置
- 可用性单一门 `evaluateEngineAvailability`：GPU 引擎无本机 GPU 且无主机 → 不可用（带原因）；远程专属无主机 → 需主机；大权重 → **需用户同意（绝不静默下载）**
- 路由决策 `engine-routing`：PDB=实验测量 / AlphaFold DB=**数据库预测（不是实验结构）**；折叠无本机 GPU → **远程作业且必须批准**；ΔΔG 物理法只走远程 + 批准，CPU 预测器需先同意且必须标"预测值"；都不行 → **not computed + 具体原因**（禁定性替代）
- agent 侧投影：compute 技能列出可用引擎与阻塞原因，并写死规则（未批准不得下载/提交；预测必标注；无引擎就说未计算）

**执行基准扩容**：SciBench-Local 5→7 案例、9→13 规则（G6 子集标注、G1 审批门、引擎点名、预测标注——"沉默给数"与"预测当测量"均判失败）

- **质量基线**：双配置 typecheck 干净；figure/omics/engine/sci-bench/compute 新增面全绿（compute 簇 526 例）；Nightly + Windows CI 双绿

## v1.54.0 — 2026-09-12（大文件先探后算 · 全量提案 · 追溯报告 · 附件版本）

承接五项升级，本版补齐"大文件（h5ad/VCF）怎么安全地看、怎么合规地算、怎么把过程交付出去"这一整条链，并把技能许可判定的默认口径从"放行"改成"先确认"：

- **大文件先探后算（h5ad / VCF）**：新增预览契约 `OmicsPreviewManifest`（格式识别、全量计数、显式 `subset{请求/实际/抽样方式}`、`fullRunRequired`）与技能包 `omics-data-preview`——`kernel.py` 以只读方式读 h5ad 结构（anndata → h5py → **如实报告"结构不可读"，绝不猜细胞数**），VCF/VCF.gz 仅用标准库流式计数并把截断计数标注为**下界**；子集标签由契约派生（「基于 N/M 降采样」），**降采样结论在编辑器与交付物里无法"忘记标注"**
- **预览 → 全量交接（G1）**：`canAnswerFromPreview` 对任何子集/需全量范围返回 false；`describeFullRunHandoff` 输出固定三步——提议全量作业并等待批准 → 无算力则明说「未计算：缺什么，需要什么」→ 全量回来按 provenance 标注。措辞单一来源，技能文档直接引用
- **全量作业提案**：`proposeOmicsFullRun` 生成作业简报（主机+执行模式+引擎+覆盖范围），`requiresApproval` **恒为真**、渲染文本明确"须人工确认主机与资源"、**预览已覆盖全量时拒绝提案**；缺主机/未指定引擎/总量未知一律如实列为缺口，不用猜测填充；并声明结果必须携带的标注（全量范围/引擎与版本/关键参数/输入标识）
- **技能许可 fail-closed**：审计发现既有非商用守卫 `isRestrictedLicense` **全仓零调用（死代码）**、且许可缺失或不识别时默认按可商用处理。新增 `classifySkillLicense`（`allowed`/`restricted`/**`needs-review`**：缺失或不识别即需确认）、作者 `commercial_use` 显式声明（**与许可文本冲突时报冲突而非放行**）、`requiresCommercialConfirmation` 单一决策点，并把 `licenseStatus` 接进技能目录与渲染端视图
- **执行追溯报告**：`trace-report` 把一次会话变成可审计交付件——有序步骤（完成/失败/跳过 + 可核验句柄）、产物清单、复用的检查点事实与依赖（含新鲜度）、结果范围标签、**强制诚实性说明段**（未计算项、需全量项）；markdown 与转义 HTML 双形态，并**接入既有导出流水线**（存在 `trace` 时导出报告而非对话记录，PDF 走既有 Electron 打印面，零新增 IPC）
- **验收基准扩容**：SciBench-Local 5→6 案例、9→11 规则——新增「子集必须带 N/M 标注」（G6）与「全量提案必须有审批门」（G1，且**声称"已提交/已运行"即判失败**）
- **文献附件版本历史**：替换或解除 PDF 前先落一条历史（旧文件 + 其内容指纹 + 替换时间）到新表 `ReferenceAttachmentVersion`，随列表投影最新在前（上限 20）——**复审依据不再被一次替换抹掉**；无新增 IPC 通道
- **质量基线**：双配置 typecheck 干净；omics/trace/references/sci-bench 新增面全绿；Nightly + Windows CI 双绿（期间三类 Windows 时序 flake 均本地复跑确认后 rerun 转绿，已归档处置口径）

## v1.53.0 — 2026-09-11（科学能力五项升级：算力决策链 · 图表纪律 · 检查点 · 文献批量 · 验收基准）

针对真实任务中暴露的五类失败（无 GPU 直接降级定性描述、ASCII 当图与 log 轴刻度错标、长链重启重做、文献只能单条挂 PDF、中文引文缺国标格式），本版逐项收敛，并把"是否真的解决"变成可机械判定的验收基准：

- **算力决策链（agent 侧）**：计算环境技能新增"就绪阶梯"——先列出各主机探测到的加速器/连接状态/调度能力，agent 必须依阶梯行事：提议作业并等待批准 → 用数据库预测（AlphaFold DB / PDB）且**明确标注为预测** → 仍无法产出数值时**明说"未计算：<缺什么>，需要<引擎或主机>"**。禁止用定性描述替代被请求的数值，禁止伪造；每个数值必须带引擎/版本/参数来源
- **图表纪律 5 → 7 条规则**：新增「对数轴刻度 sanity」（log 轴刻度必须为正数且严格递增，0.4 被标成 0.05 这类错位直接被抓）与「源文件双产物」（数据面板必须同时交付 .png 与 .py，最小字号 ≥ ~6pt）。两条新规则均为**告警级**——检查与建议，绝不阻断合法图
- **预览交互闭环**：图片/TIFF 预览新增**实时缩放百分比读数**（滚轮/拖拽/双击复位同步），并在切换文件时**自动复位**缩放与位移（不再继承上一张的视口）
- **任务检查点（跨会话续跑）**：项目级 `task-checkpoint.json`，记录已验证事实（如 UniProt 编号及其来源）、已装包、已算产物；agent 获得 `checkpoint_save` / `checkpoint_load` 两个工具，加载时返回**新鲜度裁决**：`fresh` 直接复用（免重检、免重装）、`stale`（输入指纹变了）必须重新核验、`missing` 从零开始。原子写入，损坏或旧版本文件按不存在处理
- **文献库批量 PDF 入册**：多选项目 PDF 一次入册——每个 PDF 建一条记录（标题取文件名、来源标注为手动文件导入）并自动挂载附件，重复命中则改为挂到已有条目而不是造重复记录；面板带 `已完成/总数` 进度与**停止**按钮
- **PDF 内容指纹**：挂载项目内 PDF 时自动记录内容指纹（前 4 MiB 的 sha256 + 精确字节数），文件被替换/重导出即可发现；解除挂载时同步清除指纹（陈旧哈希绝不描述"无文件"）。指纹计算失败不阻断挂载——溯源是增益，不是门槛
- **SciBench-Local v0（升级验收基准）**：把上述五类真实失败固化为 5 个案例 + 9 条确定性规则（算力路由/明说未计算、数值必带 provenance、figure_review 必调、图必带双产物、log 轴刻度必申报、检查点先 load 后 work 再 save、批量导入须有进度、引文须国标形态）。**不用模型打分**：规则要么被 trace 满足要么不满足，升级"完成"从此有机械判据（v1 将接入真实会话回放）
- **修复**：新增列的运行时迁移增加**表存在性守卫**——最小/老库没有该表时跳过迁移而非报错（此前会让 31 个测试文件连带失败）
- **质量基线**：双配置 typecheck 干净；references/checkpoint/sci-bench/preview 等新增面全绿；Nightly + Windows CI 双绿

## v1.52.0 — 2026-09-08（文献工作流：引文导出与 PDF 标注闭环）

延续双对照产品差距收敛，本版把「文献库」从单一收录变成可交付出稿的**文献工作流**，并完成第四项差异化红线（agent 库工具 deny-first）：

- **GB/T 7714-2015 引文导出（顺序编码制）**：中文作者原样著录、超 3 人按文献语种加「, 等」/「et al.」、西文作者姓前名缩写；期刊记录 [J] 尾注 DOI，纯标识符记录 [EB/OL] 附引用日期与可解析定位符。每条引用可一键复制 GB/T 引文，或按当前视图（全部/收藏夹）导出编号清单文本——中文期刊投稿场景是参考产品的空白区
- **PDF 页码级标注闭环**：文献行内可直接挂载项目内 PDF 附件（projectFiles 面选择 + 项目作用域守卫），挂载后引用与全文在既有文件面板 PdfPreview 中联动——选中正文段落做标注时，来源定位自动携带**页码**（source locator 既有基建），综述证据可回到具体页
- **文献库 UI 全语言化**：文献库对话框 28 条文案键进 9 语言（zh/zh-Hant/ja/ko/ru/de/fr/es/en），不再硬编码中文
- **agent 库工具 deny-first（审计归档）**：grep 实证 agent 框架与 ACP 工具目录零文献库注入——引用库只经桌面 IPC 与本地 Web 命令面可达，agent 默认无任何库工具；未来若向 agent 开放必须显式走权限卡与能力门，而非默认放行
- **质量基线**：双配置 typecheck 干净；references 12+ service 用例、GB/T 格式化 7 用例、契约面计数全同步（catalog 403 / T1d 157 / internal 298 / localWeb 296 / remoteWeb 199）；Nightly + Windows CI 双绿

## v1.51.0 — 2026-09-08（文献参考库）

双对照产品差距收敛第三批：项目级**文献参考库**，把「写论文时贴链接、靠记忆拼引用」变成「检索入册、归卷入夹、查重合并」的可审计闭环：

- **标识符一键入册**：DOI / PMID / PMCID / arXiv ID 抓取权威元数据（Crossref 系 / NCBI / arXiv 标识符解析，带来源与解析时间戳的**溯源快照**随记录落库），或手动添加兜底
- **收藏夹组织**：左侧收藏夹栏即建即用，条目可归入多个收藏夹；列表按项目聚合、排序稳定
- **查重合并**：客户端按 DOI/PMID/标题归一自动分组重复项，一键合并保留首条、其余条目并入——写综述前先洗一遍数据
- **重复防护**：入册即查重，与库内已有记录冲突时明确拦截并提示，不允许静默产生脏数据
- **项目级数据模型**：Reference/ReferenceCollection/CollectionItem 三表纯增量（老库自动补表），Project 作用域隔离；参考记录沿既有权限体系（项目内全功能，库工具 deny-first 化随 agent 目录后续版本落地）
- **双操作面**：桌面 UI（工作区浮动入口）之外，全部 10 个能力（增删查、收藏夹、合并、标识符抓取）同时注册为应用命令与本地 IPC——脚本/自动化与界面同权
- **差异化点**：每条引用自带可执行核验线索（来源+时间戳溯源快照），与既有审查（Reviewer）与连接器快照基建同构，不是静态文本目录
- **质量基线**：双配置 typecheck 干净；references 主进程 17 用例 + 契约面计数全同步；workspace 渲染簇 1551 用例绿；Nightly + Windows CI 双绿

## v1.50.0 — 2026-09-07（Slurm：HPC 调度执行）

双对照产品差距收敛的 HPC 大件——远程计算主机新增 **Slurm 调度器执行模式**，为组学/结构生物学等需多日批算的场景打通集群提交：

- **执行模式切换（探测门控）**：每台 SSH 主机可在「直连 SSH」与「Slurm（sbatch）」间切换；只有探测到调度器的主机才允许切到 Slurm，探测前与无调度器主机 fail-closed 拒绝——不为不存在的调度器造开关
- **调度驱动全链路**：资源请求（partition/cpus/mem/gpus/qos/account）容错解析为 sbatch 指令；`--job-name=PureScience-<job 短 id>` 让应用任务在集群内可识别、重启后按调度器身份安全恢复；墙钟由 `--time` 表达（1 分钟下限，30 天上限）；提交/轮询/取消与既有 job 生命周期同构
- **轮询与兜底**：活性探测换 squeue、超时兜底取消换 scancel；退出码原子文件/stdout/stderr 收割/终态映射与直连模式共用同一套 poller，零重复
- **直连模式不动**：无调度器主机的 7 天超时上限、pid 句柄语义完全保持
- **compute-env 技能同步**：主机清单投影标注每台主机的执行模式，agent 拿到的环境说明与实际调度语义一致
- **质量基线**：双配置 typecheck 干净；compute 全目录 518+ 用例绿；Nightly + Windows CI 双绿

## v1.49.0 — 2026-09-07（双对照产品差距收敛批）

对照两款参考产品做差距收敛，本版交付两项能力 + 四项审计归档：

- **预览内容区右键菜单**：在预览卡片内任意位置右键，即可复制路径 / 下载 / 存为产物（此前只有标签页上有这些动作）；文件面板不弹系统菜单，与对比基线行为一致
- **模型目录保鲜**：OpenAI 目录补 GPT-6 Astra（1.05M 上下文旗舰）、Anthropic 目录补 Claude Fable 5.1（1M 上下文）——均沿厂商现有旗舰的推理强度档位
- **审查（Reviewer）四旋钮审计归档**：参考产品的四项调优（禁 thinking、排除 python 工具、锁技能、扩大工具结果上限）逐一核对——我方审查会话架构上用 REVIEWER 专用 MCP 白名单（仅 6 个只读+提交工具），python 与技能天然不可达、读取天然有界，三项被架构性消除；thinking 开关已由 v1.46 场景模型强度控件覆盖。无需为不存在的路径造空壳开关
- **技能加载审批全文展开 / 安全默认静默扩面 / 数据源许可 fail-closed 三审计归档**：技能调用（skill:invoke）走既有权限卡、数据源许可与使用条款已在各连接器描述符声明——均为面窄或已有等价机制，按"不为空壳造功能"原则记录不追平；详细结论见竞争情报文档（本地留档）

- **质量基线**：双配置 typecheck 干净；推送 CI（Nightly + Windows 全量）双绿

## v1.48.0 — 2026-09-07（岐黄：专才生态成形）

- **内置旗舰专家「自主科研主理人」**：随应用附带、只读不可更改——把研究方向变成受控、可审计、小步迭代的研究序列（澄清问题→研究契约→检索证据→获取获批数据→方案设计→分析执行→可靠性审计→方法结果写作）；开箱即用，无需安装
- **8 个研究方法技能全局化**（精选技能，主智能体与专才通用）：研究契约、文献检索策略、证据确定性分级、统计推断、研究设计、科学可视化、方法写作审计、引文核验——每项含 SKILL.md 与领域参考文献
- **专才市场首发 5 位领域专家**（应用内市场一键安装）：循证证据综合专家、临床研究设计专家、组学与生物标志物研究专家、真实世界证据研究专家、药代与剂量建模专家——全部原创中文撰写、十二段式守门、闸门+终检闭环
- **市场协议与打包三处修复**：发布根条目 digest 语义（文档 sha256 而非 zip sha）；release 文档连接器声明与选择器一致；包顶层内容白名单与 zip 重建防残留——发布管线按此实测通过
- **质量基线**：双配置 typecheck 干净；specialist 全目录 256 用例绿（含内置真实资源校验）

## v1.47.0 — 2026-09-06（质量与体验批：视觉模型独立选择修复 · 全库 lint 清零）

- **视觉模型独立选择修复**：主模型为 DeepSeek 纯文本（如 deepseek-v4-flash）时，"视觉/图像理解"模型候选此前只从存储的模型列表取（常仅剩当前主模型）导致下拉只剩"禁用"；现官方厂商候选改由内置 vendor 目录派生（DeepSeek 的多模态 deepseek-v4-flash-vision-exp 恒可选），并合并存储中刷新出的新模型
- **Hooks 依赖质量（25 处 / 14 文件）**：全库 react-hooks/exhaustive-deps 清零——依赖补齐前提是先修基建：无 LanguageProvider 时 useLanguage 回退 t 由"每渲染新建"改为模块级单例（此前任何以 t 为依赖的 effect 在测试环境都会每渲染重跑）；SpecialistsPanel 导出预览 effect 有意保持 [previewExport, view]（带注明 disable-line），NotebookPreview 以变量提取消除"复杂表达式依赖"告警
- **全仓格式化归一**：eslint --fix 全量风格统一（引号/导入序/空白），零行为变更
- **README 双语同步门禁**：pre-push 新增硬校验——README.md 与 README.en.md 的徽章/引言/发布横幅/DOI 引用必须逐字一致，防止双语文档静默分叉
- **质量基线**：eslint 全树 0 问题、双配置 typecheck 干净、settings/workspace/global-search/home 全目录回归绿（188 文件/2129 用例）

## v1.46.0 — 2026-09-06（场景行独立推理强度）

- **每行独立推理强度**：设置→模型→场景模型（会话详情/子智能体/审查）每行新增独立强度段控件——"与主模型相同"默认跟随主模型档位，也可按该行模型能力（None/低/中/高/Max…）独立钉档；无模型覆盖时选档自动以当前主模型物化，换主模型不影响已钉档位
- **场景后端共享解析器**：`resolveScenarioBackend`（override??active、场景钉档>全局档>模型默认、'default'=跟随全局、幽灵 provider 防御、场景互不泄漏），6 条单测——所有应用自建场景请求统一走此缝
- **边界如实标注**：审查/子代理等由 agent 框架内建工具执行（其模型由其自身配置决定），App 不硬注入；场景配置随设置快照经应用 MCP 提供给 agent 遵循

## v1.45.0 — 2026-09-06（修复与体验批：模型独立选择 · 更新体验 · 界面归一）

- **DeepSeek 多模态模型可独立选择（修复）**：视觉/图像理解模型候选改为逐模型判定——主模型为纯文本（如 deepseek-v4-flash）时，`deepseek-v4-flash-vision-exp` 仍可在"视觉"场景独立选用；顺带不再把纯文本模型列入视觉候选
- **CodeBuddy 安装来源**：设置→智能体 CodeBuddy 卡提供真实 npm 安装源（`@tencent-ai/codebuddy-code`，带进度与日志、装完自动重检）
- **手动安装更新提示（×9 语言）**：下载完成需手动装包时，对话框明示"安装前请先退出 PureScience"——避免拖拽时撞"应用正在运行无法替换"
- **首页可用性**：项目与最近会话列表卡片内滚动（项目再多也能下滑找到）；最近会话 5 → 10 条；项目菜单 Rename/Delete 文案键控
- **启动与品牌**：开机文案统一为"正在启动 PureScience…"（×9）；关于页移除旧标语；GitHub 双 README 引言重写为自研差异化叙述（可审计实验/自我认知代理/数据不出机）
- **设置界面归一**：凭据/记忆面板字体层级与留白对齐全站基准（13/12px 灰阶 → 标准 16/14px 层级、通栏布局）；标签/定时任务/本地模型面板改为统一的整卡列表（一体圆角卡+分隔行）与品牌色选中态、状态点改用自家 token（success/accent/danger），主按钮与开关统一品牌蓝（修复记忆面板误用琥珀 accent 的黄色按钮）
- **CodeBuddy 契约吸收**：application-command 清单 285/283/97、291 个 invoke 通道、web-api-map 重新生成，CI 全绿

## v1.44.0 — 2026-09-06（社区入口 · 原生退出确认本地化 · Release 双语补课）

- **GitHub Discussions（O5 社区入口收尾）**：仓库启用 Discussions——Q&A（使用求助）、Ideas（功能建议）、Show and tell（工作流与结果分享）、Announcements（版本公告）四类分区，置顶公告帖锚定医学主线叙事（负责人 Able · NEOlab ZeroLink 知零）；issue 模板的 Discussions 链接纠正为正确仓库地址
- **原生退出确认本地化**：renderer 崩溃/无窗口时的原生兜底对话框（两变体的消息、按钮、不再询问复选框）随系统语言在 9 种语言间切换；renderer 内模态保持精确的语言偏好处理不变
- **Release 双语补课（O1）**：v1.40.0–v1.43.0 四版 GitHub Release 正文补英文摘要段，历史正文首行即为英文版说明

## v1.43.0 — 2026-09-06（生态收口批：英文文档 · GitHub 可见性）

- **英文 README（O2）**：新增 `README.en.md` 纯英文镜像（零中文残留）——对比表 Takeaway 列、诚实边界、快速回顾、平台行等全部译尽，横幅为英文摘要；与主 README 双向链接，双文档同步维护
- **GitHub 可见性（O5）**：平台徽章（macOS｜Windows｜Linux）、内部验收 **benchmark 页**（13 项验收 L1–L7 + EGFR T790M 情报复验 + 分子动力学等，全部纯实测数据、版本钉定、附复验指引）、**Zenodo DOI 归档**（PureScience v1.43.0 已归档：10.5281/zenodo.22441075）
- **figure_review 修复**：figure RPC 网关契约对齐——review 请求按网关要求嵌套注入（sessionId/projectId/request），此前 figure_review 每次调用都报参数缺失
- **更新检查修复**：每版 Release 发布 `version.json` 客户端清单（manifest 更新流此前固定 404）；macOS 自动更新说明如实化（正式签名版与 Windows/Linux 同为就地自动重启，手动安装包仅限本地/夜间/未签名等回退场景），九语言同步
- **平台陈述纠偏**：README 与英文镜像按 Release 实况改写——macOS、Windows、Linux 三平台安装包随每版发布（此前文案停留在仅 macOS 的早期阶段）

## v1.42.0 — 2026-09-05（更新体验批：自动重启 · 状态文案明确化）

- **下载完成后自动重启应用（U2，设置项）**：设置 → 关于 新增「下载完成后自动重启应用」开关（opt-in，默认关）——开启后更新下载完成即自动重启应用完成安装；仅适用于就地更新（Windows/Linux），macOS 仍打开安装包由你运行；开关随设置持久化（全链：设置库 sanitize/存取 + IPC + 契约 pin），自动重启前仍走既有后端停机遇口
- **更新状态文案明确化（U1）**：更新弹窗 9 处硬编码英文转九语言键控；「已下载」态按更新类型给出明确下一步——就地更新显示「重启 PureScience 即可完成安装」、macOS 显示「运行安装包以完成更新」（弹窗与设置 → 关于状态行同口径），替换原先仅靠按钮暗示的模糊反馈；「正在准备更新 / 更新失败 / 关闭」等按钮与说明文案全部键控

## v1.41.0 — 2026-09-05（NVIDIA Build 内置提供商 · 切换器模糊搜索）

- **NVIDIA Build 内置提供商（G4）**：模型目录新增官方提供商 **NVIDIA Build**（build.nvidia.com 的托管开放模型网关，OpenAI 兼容 /v1 接口）——内置 6 模型目录：**Nemotron 3.5 Lightning**（加入即默认选中）+ Kimi K3 + DeepSeek V4 Flash + Qwen3.8 Flash + GLM-5.3 Flash + Hy3；取钥引导直达 Build 控制台，模型列表支持在线刷新；网关型端点不暴露推理强度选项（与 OpenCode Go / TokenHub 等既有网关提供商一致）
- **切换器模糊搜索（G5）**：composer 模型/提供商切换器的模型子菜单新增**搜索框**——输入即过滤（匹配模型名或提供商名），命中片段下划线标出并按相关度排序：提供商名命中保留其整份目录、模型名命中只留命中项、无命中显示空态提示；每次重新打开自动清空查询，避免残留过滤遮住目录；复用会话 `/` 与 `@` 弹层同款模糊匹配内核；2 枚界面键九语言同步

## v1.40.0 — 2026-09-05（Runtimes 网络防护状态卡）

- **Runtimes 网络防护状态卡**：设置 → 运行时面板顶部新增网络防护状态行——镜像「设置 → 网络」的笔记本网络访问主开关（同一持久化 egress 设置源）：开启 = 盾牌高亮「笔记本网络防护已开启」（子进程仅可访问已放行域名分组与自定义域名）；关闭 = 「网络防护未开启」提示并附**打开网络设置**直达按钮，一键前往网络区开启过滤代理；状态独立于运行时探测加载（先出状态、不阻塞发现），引导页（onboarding）等无网络设置面的场景自动退化为纯文本提示、不出现无响应按钮；含 4 枚新增 render 断言，本地全量 939 文件 / 13,132 用例与 CI 双绿验证

## v1.39.0 — 2026-09-05（产物活文档 · 会话委托控制 · 发布双语化）

- **可编辑产物（G1）**：文本产物与代码类产物从「最终快照」变「活文档」——预览导航条版本下拉（finalized-only、倒序含时间戳）任选历史版本；**Edit** 原地编辑全文（脏检查/放弃确认/失败留驻重试），每次保存经主进程 `writeUserEditedVersion` 发布为**同谱系新版本**（前版本不可变、可导航、无 agent/消息关联、立即定稿），保存后预览自动跳到新版本；**Compare** 与任意前驱做自研行级 diff（红绿增删 + 计数），无新增依赖；图片/PDF/上传/本地/Notebook 类不出现编辑入口；21 枚界面键九语言同步

- **会话级委托开关（G2，Agent 控件菜单）**：composer 代理控件新增「委托」开关（默认开，随会话持久化）——关闭后该会话内 agent 调用 delegate_tasks 子任务委托会被主进程 fail-closed 拒绝并返回明确原因，新会话不受影响；适用于不希望会话外泄上下文或触发后台子任务的场景

- **GitHub Release 正文附英文摘要（O1）**：发布脚本在存在 `dist/notes-<版本>-en.md` 时，自动在 Release 正文（中文）后附 _English summary_ 段——GitHub 页面国际访客可读；应用内更新说明仍用既有 `{zh, en}` 双语文案随界面语言切换

## Unreleased — 2026-09-02（体验与本地化批次）

- **设置导航一屏可扫**：左轨行高 32→28px、分组标题间距收紧、左轨独立滚动不裁切，面板高度提升——900px 窗口下 19 个设置项不再"滑几屏才找得到/只能靠搜索"（搜索保留为增强而非唯一路径）
- **标签可独立创建**：标签页"新建标签"不再静默 no-op——零资源标签真正持久化并自动选中，右侧显示"暂无资源携带此标签"空态（随后可在技能/连接器/专才详情页打上资源）；同步修复删除某标签时误清其它资源标签的潜在缺陷
- **首页搜索入口**：首页顶栏常驻"搜索会话和产物…"搜索框（点击即开全局搜索对话框，旁附 ⌘K 提示）——此前该能力仅有 ⌘K 快捷键可达
- **语言"跟随系统"偏好**：语言选择新增"跟随系统"且为默认值（原为仅首次启动探测后锁死）；系统/浏览器语言变更实时生效（zh/zh-Hant/en/ja/ko/fr/de/es/ru 9 语言选择器同步）
- **托盘菜单本地化**：macOS/Windows 托盘菜单与工具提示按系统语言显示（9 语言标签表，随 tray 创建生效）
- **翻译防退化门禁**：新增 `npm run i18n:coverage` 覆盖率报告与 pending 登记表机制——新增英文文案键必须当 PR 翻译进全部语言或显式登记，杜绝"新键只进中英、其余语言逐年烂尾"；本轮 9 语言新增 2 枚界面键（搜索占位、跟随系统）并同步翻译
- **第二波硬编码文案键控（第一批）**：上线 `npm run i18n:hardcoded-audit` 审计工具（扫描绕过 t() 的用户可见英文），首批键控 9 处高可见文案（数据迁移"当前（隐藏）"、审查中、连接器导出/导入的 传输方式/凭据/拖放校验、任务详情"远程工作目录"、引导页 Notebook 运行时、语言设置说明等）并九语言同步翻译；字典增至每语言 1,963 键
- **应用原生菜单本地化**：启动即安装随系统语言的九语言应用菜单（文件/编辑/显示/窗口 + macOS 应用菜单，role 保留原生快捷键），不再显示 Electron 默认英文菜单
- **凭据页三段式对齐**：设置→凭据按 服务（供应商卡片+密钥状态）/ 连接器凭据（空态+新建/管理）/ 自定义（说明文案）三区组织，复用既有数据流与译文
- **网络栏代理设置**：设置→网络新增**代理**区块——默认**跟随系统**（子进程使用操作系统当前代理设置），可切换**手动配置**并指定 类型（HTTP/HTTPS/SOCKS5）+ 服务器 + 端口 + 免代理列表（逗号分隔；localhost/127.0.0.1 始终直连）；作用于 Notebook/REPL/Bash 子进程（内核与 Shell 的 HTTP(S)/ALL_PROXY/NO_PROXY 环境注入，egress 出口白名单开启时以过滤代理优先、手动代理让位），重启后保持生效；网络出口白名单卡片同步更名对齐为"笔记本网络访问"（含"已允许 N 个自定义域名"计数摘要，说明文案改写为参考心智模型）；新增 23 枚界面键九语言同步（字典每语言 1,994 键）
- **模型栏场景模型分组**：设置→模型新增**场景模型**区（主模型与视觉模型之间）——会话详情 / 子智能体 / 审查 三行各自可选默认模型（默认**与主模型相同**，可单独指定任意已配置供应商的模型，选择器与视觉模型同款分组样式）；存储为 `scenarioModels` 设置映射（含 sanitize 门禁：仅已知场景 id + 非空 provider/model，非法载荷回退继承主模型）；如实标注框架边界——当智能体框架经 ACP 运行时，子智能体与审查的逐会话模型下发受框架限制（ACP 会话创建无 per-session model 字段），届时回退主模型（对应供应商模型/推理强度联动待框架层支持后接线）；新增 7 枚界面键九语言同步（字典每语言 2,001 键）
- **七语言全量翻译至 100%**：zh-Hant/ja/ko/fr/de/es/ru 字典由 6–7% 覆盖补齐至**每语言 1,954 键全量**（约 1.28 万条译文）——含拼接片段键、`{占位符}` 与 `<code>` 标签保真、嵌套词（其/its）按语言语法处理；配套质量门禁同步上线：zh-Hant 简体字残留扫描、跨语言"值=英文"白名单（同形词/借词/占位符）、pending 登记表全部清空——切换任意语言不再大面积回退英文
- **首页项目行文件类型徽标（T5 首项）**：每个项目行在名称右侧显示该项目 Files 目录中最近产物的小型类型 chip（MD/DOCX/CSV/PNG/PDB…，至多 4 个、按新近去重、窄屏隐藏）——对齐参考产品 20.06.15 的信息密度；数据来自项目文件索引的单次轻量读取，web 无桥接或空目录时自动不渲染；无新增文案键
- **专才市场排序控件（T5b）**：市场列表新增排序下拉——**最新版本**（semver 逐段降序，同一来源内等价"最近更新"，默认）/ **名称**（忽略大小写升序），等值保持原快照顺序；说明：市场快照不含 release 日期（来源按 git ref 解析版本），"按时间最近更新"需新增 release 日期采集链路，属后续专项；排序逻辑为无依赖纯函数并带 4 条单测；新增 3 枚界面键九语言同步（字典每语言 2,004 键）
- **多语言残留清理批次（2026-09-04）**：
  - 首页与开机标牌：**"Beta" 改为软件版本号**（v1.37.0，随更新信息显示；版本号本身无需翻译）
  - **相对时间本地化**：列表中的紧凑相对时间随界面语言显示本地单位（中文"2周"而非"2w"；zh-Hant/ja/ko/fr/de/es/ru 各有单位表，en 保持缩写；i18n provider 切换语言即同步）
  - **凭据页三段式文案键控**：服务 / 连接器凭据（设备级共享凭据说明 + "暂无连接器凭据。"空态）/ 自定义（"凭据保留在各自配置中…"说明）——此前整段硬编码英文，现 5 枚新键九语言
  - **记忆内置分类本地化**："About you" 在用户未改名时按界面语言显示（中文"关于你"等）；用户改名后以自定义名为准
  - **视觉模型空态**：无支持图像输入的供应商时给出说明文案（"当前没有支持图像输入的模型可选…"）而非空无一物
  - **远程控制面板残留键控**：canManage 提示（含配对管理版）、App/浏览器两种访问的引导段、步骤 2/3、"配对请求"标题、"拒绝"、请求时间（"{time}" 插值）；删除跟随中文整句后的重复英文尾巴（"and does not include…"为键值重复拼接）
  - 本批新增 16 枚界面键九语言同步（字典每语言 2,020 键）
- **对话工作区残留键控（2026-09-04，第二批）**：
  - **Usage 用量弹层**：Input/Cache read/Cache write/Cache/Output 段标签、轮次数（"1 轮/3 turns" 按语言）、智能体/模型前缀、"此响应的词元用量"等无障碍文案全部键控（原硬编码英文）；aria 总计句保持 {total} 插值
  - **消息时间本地化**：气泡时间与悬浮完整时间由界面语言驱动（中文"8月19日 1:21"而非 en-US "Aug 19"），新增 ui-locale 注册表随 i18n provider 同步
  - **输入框提示本地化**："/ 技能 · @ 文件 · # 会话 · ⌘K 搜索 · ↑↓ 历史"（en 保持原样，视觉基线不受影响）
  - 本批新增 18 枚界面键九语言同步（字典每语言 2,037 键）
- **第四批组 2（运行环境/技能详情）**：App 托管环境标题、禁用对话框（标题/两条影响说明含 {active}{running}{idle} 插值/停止运行/当前工作结束后禁用/Cancel）、技能详情 Details 标题与 Author/License 键控；禁用影响文案经 zh 机翻腔门禁修正（该会话→此会话）；9 键 × 9 语言（字典每语言 2,111 键）；键控同时核验交互闭环：Stop/Disable 按钮分别绑定 confirmForceStop(强制 abort 并记 cancelled)/confirmDisable(排空后关内核)
- **第四批组 3a（对话区/生命周期）**：运行失败区"报告错误"按钮（aria 已有键）、拖放附加浮层提示、LifecycleToast View/关闭 aria 键控；4 键 × 9 语言（字典每语言 2,115 键）；审查：Report error→openReportDialog 对话框入口存在、Toast onView/onDismiss + 6s 自动关均接真回调
- **第四批组 3b（审查者/验证清单/折叠时间线）**：Go to transcript（ReviewerCard+清单共用）、Re-running/Re-run review、Mark addressed、Verification checklist、"{open} 项待处理，共 {total} 项"、折叠上下文/预览/折叠计数（{n} fold{s}）、折叠原因标签（自动/手动/溢出恢复，原 REASON_LABELS 英文硬映射）、时间随界面语言（toLocaleString(getUiLocale)）；12 键 × 9 语言（字典每语言 2,127 键）；审查：清单行 Mark addressed/Reopen 绑定 onMarkAddressed/onReopen、Go to transcript 均接 GoToTranscriptIntent 链路
- **第四批组 4a（模型选择器/预览/写入审计）**：无可用模型 aria、"打开设置"菜单项、审查视图 tablist aria（两处）、写入审计标题与"N 个文件变更—新建/修改/删除"摘要（{n}/{s}/{created}… 插值）；5 键 × 9 语言（字典每语言 2,132 键）；审查：无模型按钮 openSettings 打开设置对话框链路、tab 切换三视图均有对应面板组件
- **第四批组 4b（存储/审查者日志）**：存储页新位置标签、改位置确认对话框（标题/说明）、采用文件夹对话框（标题/重启按现状提示/缺失不可用说明）、Continue/Cancel、采用按钮复用 settings.useThisFolder；SessionReviewer 审查日志标题与展开/收起；10 键 × 9 语言（字典每语言 2,142 键）；审查：改位置经 StorageMigrationModal 真实迁移、adopt 仅切 dataRoot 重启、审查日志空则整段隐藏（acceptance 空态）
- **第四批组 4c（专才切换详情/全局搜索 aria）**：Main Agent 命名与"恢复默认智能体"说明、专才停用拒批提示、名称解析失败提示、Full access/所选能力、打开配置 aria、Disabled 徽标；全局搜索会话 Hash aria 键控；8 键 × 9 语言（字典每语言 2,150 键）；审查：详情块点击打开专才配置(openSettingsToSpecialist)、解析失败/停用态与主进程 getByName 校验身份一致（代码注释佐证）
- **第四批组 4d1（数据根目录缺失/旧版数据迁移对话框）**：仍未找到提示、迁移标题/说明/按钮；3 键 × 9 语言（字典每语言 2,153 键）；审查：迁移动作经 StorageMigrationModal 真实迁移并保留设置与历史
- **第四批组 4d2（文件浏览器）**：在访达中显示、目录截断说明；2 键 × 9 语言（字典每语言 2,155 键）；审查：Show in Finder 经 handleReveal 调系统访达揭示、5,000 截断为真实服务端限制并有子目录导航提示
- **第四批组 4e-1（技能编辑器主体）**：Identity/Description/Content/References 区块标题、技能 ID 标签（复用 settings.skillId）、用途说明、粘贴 SKILL.md 提示（前后段包裹 --- 代码元素）、拖放以上传浮层、上传标题与说明、支持文件说明、references/ 保存尾句、移除引用 aria；13 键 × 9 语言（字典每语言 2,168 键）；审查：粘贴自动解析 frontmatter 填充字段（既有）、引用文件经 input multiple onChange 添加、移除 X 按钮真删引用
- **第四批组 4e-2a（专才面板）**：导入 ZIP 提示/选择 ZIP/下载 JSON/只读与内置不可改说明/Capabilities 复用键/需先开项目提示/预览后编辑器完成/包检查器 Compressed·Uncompressed·Diagnostics·Target·Custom 分区标题；12 键 × 9 语言（字典每语言 2,180 键）；审查：Choose ZIP 经 selectPackage 真导入预览、Download JSON 导出报告、内置专才只读态由源码数据源标记
- **第四批组 4e-2b（提供商列表/表单）**：重新导入 Codex 登录、断开 PureScience 连接、Model: 前缀（复用）、N 个模型计数、Base URL、Endpoint aria；6 键 × 9 语言（字典每语言 2,187 键）；审查：重新导入/断开按钮分别接 onReimportCodexAuthentication/onLogoutSharedClaude 真回调
- **第四批组 4f-a（文件浏览/计算主机/OAuth/修复对话框）**：本地文件跳转/固定/重读提示、修复 aria、Compute 添加主机（从 ~/.ssh/config 选择说明整段单键 {cfg} 占位、修复了原 zh 独立成句缺失否定语义的 sshCredentialsCopied 误译并移除该键、From ~/.ssh/config 标签、选择 aria、或输入别名）、Claude 隔离登录 OAuth 提示分段；12 键 × 9 语言（字典每语言 2,196 键）；审查：跳转/固定/重读均有对应动作处理、主机选择接 Select onChange 与 SSH 别名加载
- **第四批组 4f-b（存储迁移文案/结构预览）**：adopt/migrate 提示段落整段键控（adopt 前后段包裹既有 strong 键、migrate 加粗 {size} 占位、重建说明修复 Python/R 首用重建语义）、分子/PDB 查看器标题与原子计数；7 键 × 9 语言（字典每语言 2,202 键）；审查：迁移动作延续既有 StorageMigrationModal；预览层 PreviewFallback 的 Preparing 系列文案（17 处）与缺失/越界文案留 4f-b2 专项（诚实记录）
- **第四批组 4f-b2（预览加载/错误文案）**：17 种格式的"正在准备…"加载文案（Preparing code/data/sequence/…全部键控，格式名经 key 映射）、文件缺失/存储外错误说明键控；19 键 × 9 语言（字典每语言 2,223 键）；审查：加载标题在预览运行时组件经 t 解析、缺失/越界文案保留原语义（移至字典）
- **开机加载文案键控（组 1b）**：设置加载门"正在加载设置…"、重试、会话恢复门"正在加载已保存的会话…"随界面语言（语言在首帧即从本地存储解析，不受设置加载影响）；4 键 × 9 语言（字典每语言 2,102 键）
- **第四批组 1（JobDetail/权限面板）**：任务详情弹层 Job ID/提供商/状态/运行时标签、权限面板“全部撤销”按钮+aria、“默认使用完整访问权限？”对话框标题/确认钮/取消键控（9 键 × 9 语言，字典每语言 2,098 键）
- **远程控制/全局搜索/Web 搜索行残留键控（2026-09-04，第三批，一次到位）**：
  - **主进程错误消息全量本地化**：`src/main/remote-access/*` 抛出的 33 条英文错误（Remote.It 服务创建/切换、端点、云配置、本地服务停止、"应用未安装…请重新检测"等）经 `remote-error-catalog` 精确映射回字典键，界面按当前语言显示（含 zh-Hant/ja/ko/fr/de/es/ru 全文翻译）
  - **远程控制面板补齐**：not-installed 红警、App 步骤 4 与浏览器三步引导（含"信任此浏览器"引号内嵌）、"设置完成后链接与二维码显示在这里"待就绪段、Open 按钮、受信任浏览器"上次使用 {time}"与撤销 aria、加载文案——本文件已无英文残留
  - **全局搜索对话框**：占位符（项目内/全部）、结果计数（"{n} 条结果"）、"正在搜索产物…"、Artifacts/Sessions/Commands 分区 aria 键控
  - **Web 搜索活动行**：工具名"Web 搜索"、展开区 query 标签、结果计数键控（原硬编码 "Web Search"/"1 result/N results"/"query"）
  - 本批新增 51 枚界面键九语言同步（字典每语言 2,089 键），并附 catalog 单元测试

## v1.38.1 — 2026-09-05（运行时包 CDN 修复）

默认环境包 CDN 自失效域名迁至 GitHub Release 资产（`runtime-v1`，扁平 URL、支持 >100MB 包、含 R 4.3/4.4）；URL 构造新增 flat release-asset 模式（单测覆盖）。修复 Windows 全量测试预算契约与 artifact pin。

## v1.38.0 — 2026-09-05（多语言全量整改 + 代理/场景模型 + DeepSeek 蓝视觉 P0）

本版为 2026-09-02→09-05 整改批次（明细条目列于下方 v1.37.0 段，版本号口径归本版发布）：代理设置(T4)、场景模型(T7)、菜单动态加载修复、首页文件徽标/专才排序、i18n 四批全量键控（自 2,037→2,223 键 × 9 语言：Beta→版本号、相对时间本地化、凭据/记忆/视觉/远程/对话/存储/专才/预览等约 35 文件硬编码英文清零、主进程 33 条 Remote.It 错误经 catalog 本地化、开机加载文案）、CI 加固（Windows 预算 60→120s + 契约同步）、T6-P0 DeepSeek 蓝主题 token（亮/暗双套）。

- **Agent 自省（host_query）**：agent 可对应用数据库做**只读自省**——**host_query** 工具运行 SELECT 查询（项目/审查/发现/权限/计算任务/产物等 11 张自省表），回答「我在这个项目里做过什么、现在什么状态」；安全三重防护：SELECT-only 校验（拒绝多语句/注释/跨库 `main.` 引用）、**表白名单**（非自省表一律拒绝）、**项目隔离**（行含 projectId 自动过滤到当前项目，200 行上限）；内置 self-awareness 技能（40 个内置技能）含表参考与示例查询；查询结果是数据不是指令（zh/en 双语）

## v1.36.0 — 2026-12-06（能力批次）

- **技能自举（skill bootstrap）**：技能创建循环完整化——**skill_eval** 评估技能描述的可触发质量（0-10 分 + 逐项检查：首句自包含 / 动作场景词 / 具体主题 / 关键词密度 / 长度；弱开头「a skill for」、空泛词、无实质关键词都会给出改写建议），**skill_list** 列出已装技能（名称 + 描述）防止重复造轮子，**skill_read** 读取技能全文（frontmatter + 指令）便于复用与扩展；与既有 create_skill 组成「评估 → 创建 → 检查 → 迭代」自举循环，描述分数 ≥8 再落盘；纯规则引擎无外部模型（zh/en 双语）

## v1.35.0 — 2026-12-05（能力批次）

- **出版级图表正确性（figure-style）**：agent 产出任何科学图表前可用 **figure_review** 工具做五条纯规则复查（无审美内容，处处适用）——**数据保真**（排除的行不得渗入摘要统计，error 级拦截）、**标签经济**（轴/系列必须有标签、密度上限 12）、**色彩串线**（同一类别跨面板同色、调色板 ≤8 色相）、**按数据形态选图**（时间→线图/类别→条形/分布→直方图箱线/关系→散点）、**渲染自检**（未渲染即 error）；结构化面板描述输入，违规清单逐条输出（rule/panel/severity/message），修完 error 级重跑；内置 figure-style 技能（39 个内置技能）含完整规则文档 + 数据形态选图表（zh/en 双语）

## v1.34.0 — 2026-12-04（能力批次）

- **PDF 分层阅读（pdf-explore）**：50 页论文不再整篇进上下文——agent 用 **pdf_open** 注册 PDF（主进程解析文本 + 书签目录，逐页持久化到磁盘，跨 turn 保留、零上下文占用），**pdf_outline** 查看目录（标题 + 页码 + 层级），**pdf_scan** 按词频相关度扫描定位内容（数据集名/指标/术语 → 排序页 + 得分 + 片段），**pdf_pages** 只读取需要的页范围（1-indexed）；扫描件（无文本层）自动识别空页并提示 OCR；页文本 6000 字符/页截断、全文档 100 万字符与 500 页上限保护仓库（zh/en 双语）

## v1.33.0 — 2026-12-03（能力批次）

- **文件注解（file annotations）**：agent 可通过新 MCP 工具 **annotation_set** 给项目文件打带标签的注解（todo/question/important/note/review + 简短备注，路径相对项目根，同标签重复设置自动替换），**annotation_list** 查看（可按文件过滤），**annotation_remove** 删除；注解锚定内容版本（可选 file_sha256，检测内容漂移）；路径安全校验拒绝绝对路径与 .. 逃逸；文件面板每行新增注解按钮（hover 显示），弹窗内可视化查看/添加/删除（zh/en 双语）
- **模型 runbook 扩充**：新增 **生物模型本地托管**（managed-bio-endpoints）——通用托管模式（FastAPI 包装 + 幂等启停 + 就绪路由 + 权重缓存挂载）+ fair-esm2 轻量示例与 esmfold2 GPU 示例；连同 llamacpp-local-server 共 2 个本地托管 runbook，38 个内置技能

## v1.32.0 — 2026-12-02（能力批次）

- **本地模型服务（managed endpoints）**：agent 可通过新 MCP 工具 **endpoint_register** 注册本地模型服务（名称 + 回环地址 + 模型技能 + 幂等启停脚本 + 就绪路由），**endpoint_free_port** 从托管区间（20000-29999）分配端口，**endpoint_start/endpoint_stop** 掌控生命周期，**endpoint_status/endpoint_list** 查看状态；主进程守护生命周期——首次注册的脚本集需在设置 → 本地模型面板批准（sha256 白名单，字节相同静默通过，任何改动重新审批），启动后轮询就绪路由，就绪才放行调用，连续失败进入 failed 并记录 last_error；凭据值经环境变量注入启动脚本（agent 端永不见明文）；内置 llama.cpp 本地推理 runbook（zh/en 双语）
- **设置 → 本地模型面板**：注册表单（名称/回环地址/模型技能/凭据/启停脚本/就绪路由）+ 生命周期卡片（运行中/启动中/失败/已停止 + 启停/删除）（zh/en 双语）

## v1.31.0 — 2026-12-01（能力批次）

- **定时任务（scheduled tasks）**：agent 可通过新 MCP 工具 **routine_configure** 编排周期任务（自包含指令 + 5–1440 分钟间隔），**routine_status** 查看计划与执行结果，**routine_cancel** 取消；主进程 30 秒节拍调度器到点后把指令派发为全新任务运行（沿用任务运行器，不引入新执行引擎），连续 3 次失败自动暂停（reason=stuck）防止失效任务空转；设置 → 定时任务面板提供可视化新建/暂停/恢复/删除（zh/en 双语）
- **数据源许可门控（license gate）**：connector 工具可声明 `noncommercialOnly`——CADD 变异危害评分（非商业用途限制）已标记；在「使用目的」为商业用途时调用受限工具会 fail-closed 拒绝并给出明确指引（切换到非商业即可启用），合规边界从"文档说明"升级为"调用期强制"（zh/en 双语）

## v1.30.0 — 2026-11-28（能力批次）

- **任务并发上限（task concurrency ceiling）**：任务运行器新增全局并发上限——同时运行的任务数可配置约束，超限时拒绝新任务并提示「Too many concurrent task runs」；运行完成自动释放配额；每会话单运行串行保持不变，此上限跨会话生效（zh/en 双语）
- **Agent 档案生态确认**：个人专才档案（自定义名称/描述/系统提示/图标/能力模式/技能绑定）已支持全生命周期（创建/编辑/启用/复制/导出/会话绑定）——本次审计确认端到端可用，无需改动；SSH 密码认证（凭据面板 + SSH_ASKPASS 注入）与主机级并发上限亦已完备

## v1.29.0 — 2026-11-26（能力批次）

- **Deny-first 网络防护（deny-first egress）**：内置外泄域名黑名单（云元数据端点——AWS/GCP/Alibaba 凭据外泄目标），**deny 永远优先于 allow**——即使网络白名单关闭或用户手动添加了黑名单域名，这些端点也一律 403；HTTP 与 HTTPS CONNECT 隧道双路径生效（zh/en 双语）
- **写入审计（write audit）**：notebook 工具新增「写入审计」面板——聚合每次代码/Shell 执行产生的文件变更（路径/变更类型/大小/时间/所属运行），支持按 创建/修改/删除 筛选；工作文件观察器新增变更类型检测（含此前未捕获的**删除**事件），让"agent 写了什么"一目了然

## v1.28.0 — 2026-11-24（能力批次）

- **记忆溯源（memory provenance）**：记忆笔记支持来源证据——agent 保存笔记时可附带 `evidence`（来源说明，如来自哪个会话/产物），记忆面板显示「来源」标签；被取代的笔记标记「已取代」且**不再被自动回忆**（保留可见可编辑，避免上下文矛盾）；`memory_save_note` MCP 工具新增可选 `evidence` 参数，持久化层保留溯源字段（zh/en 双语）
- **Artifact 环境证据确认**：产物证据体系已含完整环境快照（`ArtifactVersionEnvironmentEvidence`：包清单/版本/生态/加载状态 + code/execution/environment 三视图）——本次审计确认端到端可用，无需改动

## v1.27.0 — 2026-11-22（能力批次）

- **科学数据补齐（science data gap closure）**：connector 目录新增 3 个数据源——**CADD**（变异危害评分，PHRED/raw，单变异与位点查询，GRCh37/38 多版本）、**DepMap**（癌细胞系依赖性评分 Chronos，细胞系搜索 + 基因依赖性排行）、**PanglaoDB 规范标记基因**（离线内置，单细胞注释：细胞类型→标记基因、基因→细胞类型、类型清单，免疫/基质/上皮/器官特异全覆盖）；「变异」「表达」「癌症模型」connector 的目录元数据同步更新（zh/en 双语）

## v1.26.0 — 2026-11-20（能力批次）

- **可检索长上下文（searchable long context）**：上下文压缩不再丢弃原文——每次压缩时被折叠的对话窗口持久化为不可变 chunk，agent 可通过新 MCP 工具 **summary_query** 检索折叠内容的逐字细节（精确错误信息/文件路径/数值/决策），**boundary** 工具让 agent 标记任务边界（折叠落在任务之间）；审查工具新增「Folded context」标签页展示折叠时间线（时间/原因/边界标签/token 数，可展开摘要与原文预览）；能力开关随会话 capability 自动注入系统提示（zh/en 双语）

## v1.25.0 — 2026-11-18（能力批次）

- **核验清单（verification checklist）**：审查工具新增「清单」视图——跨 review 聚合会话内全部 warn/fail 声明（claim），每条显示最新判定（pass/warn/fail）/证据/重标记次数/被评估次数，支持一键「标记已解决 / 重新打开」；会话无审查记录时自动展示清单入口（zh/en 双语）

## v1.24.0 — 2026-11-16（能力批次）

- **子任务模型覆盖（sub-agent model override）**：任务/子代理会话创建支持指定模型——`StartTaskRunRequest.modelId` 经任务运行器透传至会话创建，会话配置器优先使用请求的模型而非后端当前模型（delegate/reviewer 子任务可独立选择模型）；向后兼容，未指定时沿用现有行为

## v1.23.0 — 2026-11-14（能力批次）

- **Usage 按运行归属（per-run usage）**：设置 → 使用量 新增「按运行查看」区块——每次运行（root 会话帧）显示实际消耗 token，委托子运行（sub-agent/delegate/reviewer）的用量自动归并到所属主运行并标注子运行数，帮助定位高消耗任务；无会话图的旧会话自动合成单条运行（zh/en 双语）

## v1.22.0 — 2026-11-12（能力批次）

- **更新下载加固（update hardening）**：更新清单（manifest）解析新增下载来源白名单——仅允许官方域名与 GitHub Releases 资产域（https 强制），被篡改/非白名单 URL 在解析阶段即拒绝；manifest 拉取新增 10 秒超时，防止更新流程被挂起的请求阻塞

## v1.21.0 — 2026-11-10（能力批次）

- **凭据引导恢复（guided recovery）**：凭据验证结果新增失败分类（auth/network/format/unknown）并按类显示针对性恢复提示（密钥失效重输/网络不可达/格式错误等）；编辑凭据保存后自动重新探测——通过即关闭，失败则停留在编辑框内给出恢复指引，闭环修复（zh/en 双语）

## v1.20.0 — 2026-11-08（能力批次）

- **许可使用声明（use intent）**：设置 → 通用 新增「使用目的」——商业用途（默认）/非商业用途；启用声明为限制性许可（如 CC-BY-NC）的技能时，商业模式下先弹出许可确认对话框，确认后才启用（zh/en 双语）

## v1.19.0 — 2026-11-05（能力批次）

- **第三方许可证面板**：设置 → 关于 新增「第三方许可证」入口——列出应用依赖的开源软件许可证摘要（取自安装依赖树，名称/版本/许可证文本），便于合规审查；主进程收集器 + 新 IPC 通道 + 对话框 UI（zh/en 双语）

## v1.18.0 — 2026-11-03（UI 交互批次）

- **会话未读续接引导**：侧栏会话列表显示「新」徽标——会话在你上次阅读后有新活动时出现（基于上次打开时间，localStorage 持久化）；打开会话自动标记已读；活动会话永不显示未读
- **模型选择器不可用原因本地化**：灰置模型/组的完整原因文案（Codex 桥不支持等）走 i18n；i18n 引擎新增 `{变量}` 插值支持（t(key, vars)，向后兼容）
- **Usage 面板确认对齐**：token 用量时间范围控件（24h/7 天/30 天切换、响应式短标签）确认与参考产品一致

## v1.17.0 — 2026-10-31（中文翻译覆盖度审计批次）

- **硬编码英文全量补齐**：全库审计发现 234 处未走 i18n 的用户可见文案（文本节点/placeholder/aria-label/title 全类型），逐面板移入双语字典（新增约 190 个键，en/zh 同步），覆盖设置/工作区/预览/引导流程/组件对话框等全部界面
- **术语一致性修复**：全界面「专家」→「专才」统一（术语表 specialist=专才，34 处误用修正，含面板/导入/搜索/编辑器等全部场景）
- **机翻腔抽查**：全量扫描被动语态直译/英文句式残留，现有文案无严重问题；术语表回填 4 条（权限模式/配置预览/配置诊断/仓库结果）

## v1.16.0 — 2026-10-27（P3 生态与提供商 · 4.3）

- **MCP 标准配置导入导出**：导入兼容**宿主文件多形态**——Claude Desktop 的 `claude_desktop_config.json`（顶层 `mcpServers` 嵌套 + 无关键自动忽略）、Cursor 的 `mcp.json`、裸 `mcpServers.json` 均可直接导入；导出保持标准 `mcpServers` 形状且凭证以 `${NAME}` 占位符脱敏，可安全分享
- **全量收尾**：Phase 3/4 全量回归（测试/typecheck/build/打包验证）、术语表终审回填（模型网关/目录标签/图片区域注解/MCP 配置等 8 条）

## v1.15.0 — 2026-10-22（P3 生态与提供商 · 4.2）

- **Grok 三协议全通**：新增本地 Messages→Responses 翻译桥——Claude Code 的 Anthropic 协议请求经本地代理翻译为 xAI 原生 Responses 请求（消息/工具/流式事件双向映射），Grok 订阅（OAuth）与 xAI API Key 均可驱动 Claude Code，至此 Grok 在 Claude Code / OpenCode / Codex 三个框架下全部可用
- **DeepSeek V4 Pro Responses 直连**：V4 Pro GA（2026-08-13）原生 Responses API——`deepseek-v4-pro`（含 `[1m]` 变体）不再走 Chat Completions 桥，与 V4 Flash 一样直连 `/v1/responses`
- **GitHub 认证技能导入**：技能导入/预览/仓库扫描/搜索自动携带统一凭据中的 GitHub token（仅注入 github.com 与 api.github.com，限流提升 + 私有仓库导入），凭据验证/保存/替换复用统一凭据面板

## v1.14.0 — 2026-10-19（P3 生态与提供商 · 4.1）

- **模型目录扩充**：新增 **OpenCode Zen** 精选网关（GPT-5.x / Claude / Gemini / Grok / 免费模型等 23 个精选条目，含原生 Responses 端点路由）与 **OpenCode Go** 配额计划目录（MiniMax / Kimi / GLM-5.3 / DeepSeek V4 / Qwen3.8 / Hy3 等 21 个条目）
- **腾讯 TokenHub 提供商**：腾讯大模型服务平台（原混元 / DeepSeek API 入口迁移目标）——DeepSeek V4 Pro/Flash、Hy3、GLM-5.3、Kimi-K3、MiniMax-M3 等 13 个模型，Hy3 与 DeepSeek V4 家族支持原生 Responses 路由
- **混合协议端点路由**：Zen 的 GPT-5.x 家族走 `/zen/v1/responses` 原生端点、其余模型走 Chat Completions——per-model 端点元数据驱动，live 模型刷新同样覆盖

## v1.13.0 — 2026-10-14（P2 体验打磨 · 3.4）

- **注解图片版补全**：agent 生成的图片支持框选区域注解——点「框选图片区域」进入框选模式，拖拽圈定区域；选中后可**放大 2 倍精调边界**（四角手柄微调）、**重选**或**取消**；确认后生成图片区域注解卡（含归一化坐标），随下一条消息作为引用上下文发送给 agent
- **注解卡升级**：图片区域注解显示图标徽标与坐标信息；消息内注解 chips 区分文本注解与图片区域注解
- **持久化兼容**：注解契约扩展为 text / image 判别联合，图片注解经严格校验（区域合法性与边界钳制）后随会话持久化，非法数据 fail-closed 丢弃

## v1.12.0 — 2026-10-12（P2 体验打磨 · 3.3）

- **跨资源目录标签与收藏**：技能、连接器、专才三大目录统一标签模型——每项可打标签（≤8 个，去重、规范化）、可收藏；任一目录行内直接添加/移除标签，星标一键收藏，localStorage 持久化
- **目录过滤条**：全部 / 收藏 / 标签 chips 三段式过滤（标签与收藏 AND 组合，多标签叠加）；chips 基于当前视图自动汇集已有标签，随过滤实时收窄
- **技能面板重设计**：范围分段（全部 / 主 agent / 专才技能，按专才有效技能绑定自动归组，专才徽章标注）；紧凑工具栏 + 批量操作模式——全选、批量启用/禁用、批量删除（内置技能保护跳过并计数提示），`Promise.allSettled` 逐项容错
- **连接器与专才面板同步升级**：收藏 + 标签 + 过滤条全线接入，与技能目录一致

## v1.11.0 — 2026-10-10（P2 体验打磨 · 3.2）

- **run-marks 轮次导航轨**：长对话（≥8 轮）右侧竖向圆点导航——每个用户轮次一个标记，随内容比例分布；当前阅读位置高亮；点击任意圆点平滑跳转到该轮起点；千轮长对话可快速定位，短对话自动隐藏
- **会话导出按轮次**：导出对话框支持 Markdown/PDF 双格式 + 三态轮次选择（全部轮次 / 指定范围 from–to / 单轮）；轮次超界自动收敛到实际轮数；范围非法时禁用导出并提示；保留基础全量导出路径不变

## v1.10.0 — 2026-10-08（P2 体验打磨 · Phase 3 之始）

- **会话 hover 预览卡**：侧栏会话行悬停/聚焦显示即时预览卡——完整标题（长标题可滚动）、描述全文；右侧优先、放不下自动翻到左侧、底部钳制不溢出窗口；快速扫过列表不闪卡，滚动/切换会话/打开菜单即收起
- **应用内沙箱源预览**（延续）：agent 回复链接悬停显示来源卡（标题 + URL），点击在右侧沙箱 iframe 打开——HTTPS-only、无同源/无顶层导航，双闸防护在位
- 修复：HTTP-hosted skill MCP 传输补全 create_skill 接线（typecheck 恢复全绿）

## v1.7.0 — 2026-08-29

- **会话式技能创建**：会话中的 agent 可从自然语言起草技能（create_skill 工具）——名称/描述/步骤说明/引用，应用侧校验（名称格式、大小上限、引用数量）并落盘为 `SKILL.md`；agent 不直接写技能文件，持久化由应用全权负责

## v1.6.0 — 2026-08-29

- **SSH 密码认证**：远程计算主机支持密码认证（绑定凭据面板凭据 ID）——密码仅在连接时解密、经 SSH_ASKPASS 注入，绝不落盘到计算配置、绝不出现在命令行；密钥认证与 ssh-agent 路径保持不变

## v1.5.0 — 2026-08-29

- **@path 本地文件夹授权**：授权智能体读取本地目录——跨盘浏览（本地卷/路径栏）、一键授权、已授权列表管理与随时撤销；授权跨会话持久化，撤销立即失效
- 链接文件夹内的文件引用严格限定在授权根目录内（目录穿越逃逸 fail-closed）；敏感路径（密钥目录等）拒绝授权

## v1.4.0 — 2026-08-29

- **消息中心接入事件源**：收件箱开始真实记录 agent 事件——回合完成（task.completed）/回合失败（task.failed）/等待授权（authorization.required，待处理动作），桌面与 Web 共享同一后端收件箱；按（类型, 会话, 来源）去重，重试不产生重复卡片
- 收件箱标题以稳定标识存储，界面按语言映射（任务完成/任务失败/等待授权）

## v1.3.0 — 2026-08-29

- **文本注解进会话**：在转录、工具活动或文件预览中选中文本 → 「添加为注解」→ 选区作为上下文卡片随下一条消息发送给 agent（携带来源、跨重启保留、支持 edit-and-resend；每消息最多 8 张、单张 8000 字符有界）
- 注解以显式引用块（`<annotation source=…>`）注入 agent 提示，agent 视为权威用户上下文

## v1.2.0 — 2026-08-29

- **安全加固批次**：
  - 渲染器 Chromium 权限请求默认拒绝（任何 web 权限请求一律拒绝，缩小受损渲染代码的攻击面）
  - 远程计算作业输出落盘前敏感凭据脱敏（sk-/AWS/GitHub/Bearer/token= 等模式整体掩码，作业 tail 不再保留可用密钥）
  - 连接器卡死请求快速失败（30s 超时后不再重试，立即报带截止时间的错误；瞬时网络错误仍按退避重试）
  - agent 事件流有界接纳（16ms 窗口内限流高频事件，stop/error/artifact 等关键事件始终放行，万级事件回合不再冻结界面）

## v1.1.0 — 2026-08-29

- **结构化澄清工作流**：agent 可通过 ACP elicitation 发起多问题澄清——单选/多选/文本/数字/布尔字段、必填校验、跳过或提交；回答聚合后注入 agent 上下文（桌面与 Web 全链路）

## v1.0.0「浑仪」— 2026-08-29

- **版本里程碑**：独立版本号体系正式启用（v0.x → v1.0.0），与既有命名体系彻底解耦；
  本地标签空间完成治理（外部标签移入独立命名空间，git describe 恢复正确基线）

## v0.17.2 — 2026-08-28

- **Serverless GPU 与模型端点（Modal + NVIDIA NIM）**：设置 → 计算新增「Serverless GPU 与模型端点」区块——可配置 Modal serverless-GPU 端点（作业经 modal CLI 在 GPU 容器中执行）与 NVIDIA NIM 推理端点（OpenAI 兼容模型调用，可指定 Base URL 与模型名）；每个端点绑定「凭据」面板中的 Modal token / NVIDIA API key（密钥解密后注入，绝不落盘到计算配置）；作业调度器按 provider id 前缀自动路由（`modal:` / `nvidia_nim:`），提交到外部端点的作业与 SSH 作业走同一状态追踪

## v0.17.1 — 2026-08-28

- **网络出口白名单（Network egress allowlist）**：设置 → 网络新增「网络出口白名单」——总开关开启后，Notebook / REPL / Shell 子进程经本地过滤代理访问网络，仅放行 6 组科学域（文献与仓储 / 基因组与变异 / 结构与化学 / 临床试验与药物 / 生物信息工具 / 代码与软件包仓库）+ 用户自定义域名，其余一律拒绝；默认关闭（不改变现有行为），域名按后缀匹配（子域名自动涵盖）；改动即时生效于后续启动的子进程

## v0.17.0 — 2026-08-28

- **统一凭据管理（Credentials 设置页）**：为 AWS / GitHub / Google Cloud / Azure / Modal / NVIDIA / OpenAlex / 文献访问 8 类科学服务及自定义凭据提供统一存储——密钥经系统钥匙串（safeStorage）加密落盘，渲染层只见掩码提示，明文永不离本机；支持新增/编辑（不重输密钥可保留旧值）/删除（确认对话框）与连接测试（GitHub 走真实 API 校验）；设置侧栏新增「凭据」入口

## v0.16.4 — 2026-08-28

- **agent 自动保存记忆（memory_save_note）**：会话中的 agent 可通过专用 MCP 工具把符合分类保存指引的信息写入用户记忆——`purescience-memory` 服务端 + 会话级令牌 RPC 落库，主进程单写者持久化；recall 提示块附带各分类保存指引，形成「设置分类 → agent 按指引保存 → 后续会话 recall 注入」的完整闭环

## v0.16.3 — 2026-08-28

- **记忆分类可改名/删除**：分类行 hover 提供铅笔（行内改名，Enter 提交/Esc 取消）与垃圾桶（确认对话框后删除分类及其笔记）；内置「About you」可改名不可删除（归一化会自动重建）

## v0.16.2 — 2026-08-28

- **记忆 recall 注入**：记忆开启时，把各分类笔记渲染为有界提示块（每分类 20 条、每条 600 字、总预算 4000 字、超限整块丢弃）注入 agent 会话系统提示——三个框架（claude-code / opencode / codex）全部生效，记忆从「仅存储」变为「真正参与会话」

## v0.16.1 — 2026-08-28

- **记忆面板输入对齐**：新增内联「添加笔记」输入框（Enter 保存、提交后自动聚焦），笔记生来即有文字；新建分类改为完整表单（名称 + 保存时机指引 + Auto-recall 开关 + N/10 计数，Create 需名称非空）；数据清洗器允许空文本笔记（清空重打不再被丢弃）

## v0.16.0 — 2026-08-28

- **Memory 设置页（分类笔记 + 总开关 + Clear all）**：全链路新建——shared schema → 主进程 SQLite 存储 + IPC → 渲染层 store → MemoryPanel + 设置导航 + 双语文案；支持内置「About you」分类与自定义分类、每分类笔记增删改、主开关（关闭后笔记保留仍可编辑）与一键清空

## v0.15.0 — 2026-08-27

- **审阅者外部来源验证 + 跨窗口追踪**：审阅者可抓取被引用 URL 的纯文本摘要（走应用网络栈、遵循系统代理）并与被审回合的声明比对；跨窗口追踪同一证据链
- **分类导航中文深化**：设置面板分类标题与按钮中文化（General/Model/Connectors 等区块）
- **多源并行文献综述**：`literature_review_search` 聚合工具落地——提取 arXiv / OpenAlex 搜索可复用核心，单次调用并行查询多源（单源失败不拖垮整体，结果带 source 标记）

## v0.14.4 — 2026-08-22

- **更新对话框本地化**：version.json 支持双语 release notes（zh/en），更新说明跟随界面语言
- **权限拒绝强化**：用户拒绝后，agent 明确被告知无授权且不得在本回合重试或绕行（连接器/远程命令/下载审批）
- **长粘贴文本附件化**：composer 粘贴超过 10000 字符或 300 行的纯文本自动转为附件卡片（原文完整保留），不再淹没草稿

## v0.14.3 — 2026-08-22

- **xAI Grok OAuth 订阅**：新增「xAI Grok 订阅」提供商——SuperGrok / X Premium+ 账号设备码登录（RFC 8628，auth.x.ai），无需 API key；token 加密存储 + 自动刷新；OpenCode（Chat Completions）与 Codex（Responses）双路由直达 api.x.ai/v1；默认 grok-4.6
- 注：xAI 后端对 OAuth API 有订阅档位限制（部分档位登录后 403），官方 xAI API key 路径仍为可靠兜底

## v0.14.2 — 2026-08-22

- **摘要优先启动**：会话列表启动不再逐个解析完整会话 JSON——每次保存时附带写入轻量摘要缓存（指纹校验 size+mtime），启动列表直接从摘要读取；会话文件变更时自动失效回退全文。会话多时启动明显提速

## v0.14.1 — 2026-08-22

- **受管专才市场（二阶段）**：专才列表新增「市场」筛选分组；市场安装的专才在编辑器中显示受管提示（发布者内容由市场管理，自由修改请先复制本地副本）；更新可用性沿用 SemVer 严格比较
- 编辑器「标识」区补全中文化（标识/预览/实时/图标）

## v0.14.0「神农」— 2026-08-22

> 神农：辨百草，识药性——市场生态从此有源可溯、有版可查。

- **受管专才市场（一阶段）**：市场安装的专才自动标记来源（marketplace origin），列表显示「市场安装 · v版本」来源标注；市场包与本地/导入包清晰区分，为只读发布者内容与更新校验铺路
- 列表操作菜单补全中文化（复制 / 导出 ZIP）

## v0.13.9 — 2026-08-22

- **Notebook 依赖过期标记上屏**：会话 Notebook 面板中，变量被后续运行重写的单元输出显示「已过期」徽标——旧结果一眼可见，不再误导

## v0.13.8 — 2026-08-22

- **Grok 4.6 模型**：xAI 官方目录加入 grok-4.6（1M 上下文）
- **Notebook 跨运行依赖追踪**：每次运行记录脚本写入的变量，后续运行重写同名变量时自动标记该运行输出「已过期」——科研结果不再被静默的旧状态误导
- **专才市场治理基础**：来源类型支持市场安装（origin: marketplace），为受管包生命周期铺路

## v0.13.7 — 2026-08-22

- **DeepSeek 视觉模型**：deepseek-v4-flash-vision-exp 加入官方目录（Responses 直连 + 多模态图片输入）
- **自定义模型 token 上限**：最大输入/最大输出 token 独立配置（表单 + 存储 + 后端透传）

## v0.13.6 — 2026-08-22

- **中文适配深度补全**：二次排查修复 40+ 处遗漏（新建项目对话框、诊断页、远程访问、消息中心、上下文窗口、权限/审查面板、专才/计算面板、消息状态与耗时等），含中英混合文案与 i18n 重复键清理

## v0.13.5 — 2026-08-22

- **中文适配全面补全**：修复 60+ 处遗漏的英文界面文案（工作区/设置页/对话框/菜单），新增 60+ 翻译键，覆盖状态、按钮、标签、占位符、提示

## v0.13.4 — 2026-08-22

- **叙事独立化**：全仓库统一为 PureScience 自己的术语与描述体系，代码注释、文档、用户文案与标识符采用完全原创的表达——以独立项目的身份对外叙事

## v0.13.3 — 2026-08-22

- **产物自动预览**：预览面板打开时，运行完成的最新产物自动呈现（Notebook 图表直出屏幕）
- **README 品牌独立**：版本徽章/公告/品牌章节更新，官网 purescience.work + 更新日记入口

## v0.13.2 — 2026-08-22

- **关于页资源区**：帮助中心（官网）、更新日记（GitHub Releases）、报告问题（Issues）一键直达

## v0.13.1 — 2026-08-22

- **Notebook 预览**：`.ipynb` 文件原生渲染（Markdown/代码单元 + 输出与错误高亮 + 单元统计）
- **自定义 MCP 连接瞬断自愈**：服务器进程退出或连接中断时自动重连并重试，不再报硬错误

## v0.13.0 「启明」— 2026-08-21

> 启明：我们以此为界，正式确立 PureScience 的独立品牌叙事。

- **品牌独立宣言**：更新日记启用自有代号体系；全部功能以原创开源方式自主开发与维护
- **官方专才市场正式上线**：ed25519 签名验证的市场协议、GitHub 源审查添加、
  发布详情/技能选择/安装溯源全流程；首个示例专才「示例研究员」随官方市场发布
- **国内网络适配**：市场元数据与安装包经 jsDelivr CDN 分发（国内直连可用），
  官方市场在国内网络下可直接访问
- **更新分发打通**：版本更新改为经 GitHub Releases 分发（`latest/download/version.json`），
  发布一条龙脚本 `scripts/publish-release.sh`（构建 → 哈希 → 清单 → 发布 → 自动验证）
- **科学计算环境自愈**：托管运行时离线恢复（复用本地完整环境 + 就绪标记），
  不再依赖不可达的下载源；常见科学库（numpy/pandas/scipy/scanpy 等）随环境就绪
- **运行稳定性修复**：
  - 项目打开白屏根治（界面组件缺少上下文提供者导致的渲染崩溃）
  - 智能体中断后「无法继续」修复（中断引发的内部诊断按正常取消处理，会话可继续）
  - 内置技能清单完整性修复（9 个技能文档重命名对齐）
- **品牌信息统一**：仓库归属、版本号、链接全部指向 PureScience 自有标识

## v0.12.x 「星火」— 2026-08

> 星火：功能与生态的播种期。

- **v0.12.0** 专才市场初版（签名验证协议、源管理、发布安装全链路）、
  更新系统接通 GitHub Releases
- **v0.12.1** 官方市场失败关闭语义修正（无签名密钥时优雅降级，市场保持可用）
- **v0.12.2** 官方市场启用（可信签名密钥上线，发布首个示例专才）
- **v0.12.3** 官方市场 CDN 化（jsDelivr 国内加速）；运行稳定性修复

## 更早版本（v0.11 及之前）

- 多智能体协同编排（任务拆解、并行执行、上下文洞察）
- BioNexus 生物医学技能包（17 个专业技能：变异解读、药物设计、单细胞分析等）
- 全界面中文适配（约 1300 个界面文案）
- 计划工件预览、视觉中继、消息分支、使用统计看板
- 连接器生态（文献检索、生物数据库 MCP 接入）
